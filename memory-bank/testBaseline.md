# Test Baseline

Tracks the project's automated test coverage as a baseline reference. Updated whenever a feature adds new test flows.

## Current Coverage (as of #1 scaffold)

### Unit / Integration (Vitest)
| File | Tests | Covers |
|------|-------|--------|
| `tests/unit/ipc-contract.test.ts` | 4 | IPC-001 — `src/shared/ipc.ts` PING contract |
| `tests/unit/ipc-contract-claude.test.ts` | 25 | IPC-CPM-001/002/003 + drift guard — claude:* channels + IpcApi.claude type shape + PING regression |
| `tests/unit/main-ping-handler.test.ts` | 4 | IPC-002, IPC-003 — `handlePing` pure function |
| `tests/unit/scaffold.test.ts` | 8 | SCAFFOLD-001..004 — config files, scripts, strict mode, electron-builder targets |
| `tests/unit/preload-path.test.ts` | 3 | preload built as `.cjs`, electron-vite formats includes cjs, sandbox: true |
| `tests/unit/spawner.test.ts` | 18 | FakeSpawner self-tests — stream emission, exit/error events, kill, stdinWrites |
| `tests/unit/claude-process-manager.test.ts` | 24 | CPM-001..022 (+2 extras) — manager lifecycle, line buffering, cancel/timeout escalation, stdin, validation |
| `tests/unit/project-instance-schema.test.ts` | 22 | VAL-001..017 — Project Instance schema validator (per-field errors, enum, branchFormat placeholders) |
| `tests/unit/project-store.test.ts` | 16 | PS-001..015 — JSON store CRUD, atomic writes, write mutex, schema versioning, cascade delete |
| `tests/unit/secrets-manager.test.ts` | 13 | SM-001..012 — `safeStorage` wrapper, set/get/delete round-trip, plaintext-never-on-disk, backend-unavailable |
| `tests/unit/ipc-contract-projects.test.ts` | 31 | IPC-PS-001..004 — projects/secrets channels, IpcApi extension, drift guard, regression on prior contracts |
| `tests/unit/http-client.test.ts` | 6 | HTTP-001..005 — FakeHttpClient self-tests |
| `tests/unit/ticket-schema.test.ts` | 13 | TICKET-001..005 — Jira issue → Ticket mapping, missing fields, garbage input |
| `tests/unit/jira-client.test.ts` | 21 | JIRA-001..012 — search/testConnection, JQL encoding, auth header, status mapping, token-not-in-error |
| `tests/unit/run-history.test.ts` | 15 | RH-001..011 — markRunning/markProcessed/clearRunning round-trip, atomic writes, persistence |
| `tests/unit/jira-poller.test.ts` | 17 | POLLER-001..015 — start/stop/refresh, mutex, eligibility filter, back-off, AUTH short-circuit, NO_TOKEN, PROJECT_NOT_FOUND |
| `tests/unit/ipc-contract-jira.test.ts` | 27 | IPC-J-001..004 — jira:* channels, IpcApi extension, drift guard on TicketDto, full regression on prior 16 channels + 4 namespaces |
| `tests/unit/App.test.tsx` | 6 | APP-001..006 — App shell render, view routing, sidebar branding, graceful fallback when window.api missing (replaces #1's FE-001..003 — placeholder Ping demo retired) |
| `tests/unit/tokens.test.ts` | 2 | TOKEN-001 — design tokens declared on `:root` |
| `tests/unit/components-button.test.tsx` | 3 | CMP-BTN-001..003 — Button variants, onClick, disabled |
| `tests/unit/components-badge.test.tsx` | 2 | CMP-BDG-001..002 — Badge variants and pulse-dot element |
| `tests/unit/components-toggle.test.tsx` | 2 | CMP-TGL-001..002 — Toggle onChange, disabled blocks change |
| `tests/unit/views-project-list.test.tsx` | 8 | LIST-001..008 — heading, empty state, populated rows, navigation, Auto Mode toggle, loading, error+retry |
| `tests/unit/views-add-project.test.tsx` | 12 | ADD-001..012 — 4 sections, validation, secrets-then-create order, partial failures, Test Connection, mode picker |

| `tests/unit/components-tabs.test.tsx` | 3 | CMP-TABS-001..003 — controlled tablist, click + onChange, disabled tab |
| `tests/unit/components-checkbox.test.tsx` | 4 | CMP-CHK-001..004 — checked toggle, indeterminate glyph, disabled |
| `tests/unit/components-progress.test.tsx` | 2 | CMP-PROG-001..002 — fill width, value clamping |
| `tests/unit/lib-time.test.ts` | 6 | UTIL-TIME-001..006 — formatRelative bands + invalid input fallback |
| `tests/unit/lib-priority.test.ts` | 4 | UTIL-PRI-001..004 — Jira priority normalization |
| `tests/unit/views-project-detail.test.tsx` | 14 | DET-001..014 — header metadata, Auto Mode per-project, multi-select Run, master checkbox indeterminate, tabs, ticket subscription event filter, Active Execution panel |
| `tests/unit/ipc-contract-chrome.test.ts` | 12 | IPC-CHROME-001..003 — chrome:* channel strings, IpcApi.chrome shape, ChromeState/ChromeStateChangedEvent payload shapes |
| `tests/unit/components-titlebar.test.tsx` | 6 | TITLEBAR-001..006 — testid present, controls hidden on macOS, button wiring, initial maximize state reflected, live state-changed subscription, no-op when window.api missing |

**Total unit:** 1068 tests pass + 12 pre-existing skipped. #GH-38 added 63 new across 7 files: SCAN-001..008 + SCAN-PARSE-001..006 in skills-scanner.test.ts (14 covering frontmatter parsing edge cases + readdir/stat/readFile injection seam + project-overrides-user dedupe + missing-root tolerance + hidden-dotfile skip + alphabetical sort + skip-without-SKILL.md); NPX-INSTALL-001..020 in skill-npx-installer.test.ts (8 covering ref-regex rejection of shell metachars + accept matrix for owner/repo and @scoped names + argv default `npx skills add <ref> -g -y` + non-zero exit → status:failed + tail clamping to 4KB + spawn-error path + timeout-kills-child); FIND-001..031 in skill-finder.test.ts (13 covering Spawner argv + line-buffer across chunks + trailing-partial flushed on exit + exit-event reason=completed + already-active rejection + cancel-emits-cancelled-reason + cancel-with-wrong-id throws + cancel-with-no-active throws + spawn-error emits stderr line + post-exit idle and new start ok + timeout kills + active() reports state + stderr stream tagging); HOOK-SKILLS-001..005 in state-skills.test.tsx (5 covering loading:true initial + populates from list ok + sets error on list fail + refresh re-invokes + IPC-bridge-unavailable when window.api missing); VIEW-SKILLS-001..007 in views-skills.test.tsx (7 covering page header testids + empty-state with CTA + CTA opens Find dialog pre-filled `ef-feature` + table rows with name+description+Source badge + Open dispatches shell:open-path with dirPath + Refresh re-invokes list + error banner); DIALOG-FIND-001..010 in components-find-skill-dialog.test.tsx (10 covering dialog open testid + initialQuery prefill + submit dispatches findStart with query and subscribes via onFindOutput + streamed lines render in stream area + candidate regex extracts inline install cards from `- ef-feature: ...` lines + install button dispatches skills.install + manual install input + button path + Stop button replaces Search while finding and dispatches findCancel + install failure surfaces error banner + dialog close while finding cancels). Previously #GH-59 added 32 new across 4 files: NOTIF-STORE-001..009 in state-notifications.test.ts (9 covering dispatch + auto id/createdAt + dedupeKey-collision-updates-in-place + uniqueId-on-fresh-key + dismiss/dismissByKey/dismissAll + no-throw on stale/missing keys); CMP-TOAST-001..007 in components-toast.test.tsx (7 covering title + data-type-attribute mirror + close dispatch + body hidden when undefined + actions onClick + role=alert+aria-live=assertive on error/approval + role=status+polite on success); CMP-TOAST-STACK-001..006 in components-toast-stack.test.tsx (6 covering empty marker + store subscription render + maxVisible clamp keeps newest + positive-ttl scheduled dismiss + non-positive-ttl no-op + early-remove cancels pending timer); NOTIF-DISPATCH-001..010 in state-notification-dispatchers.test.tsx (10 covering done+prUrl→success+OpenPR+8 000ms ttl + failed→error+no ttl + cancelled→warning+5 000ms ttl + terminal-once-per-run-on-repeat + approval-off-route→toast + approval-on-matching-ExecutionView→suppressed + approval-set→null→dismiss + currentExecutionRunId-change→dismiss + dedupeKey-refresh-in-place + Approve-action-calls-runs.approve-IPC). #GH-57 added 3 new in components-execution-log.test.tsx (CMP-EXEC-LOG-FAB-001..003 covering scroll-to-bottom FAB hidden-at-bottom / visible-after-scroll-up / click-scrolls-and-rehydes). The GH-57 DET-WIDGET-APPROVE / DET-WIDGET-APPROVE-CLICK tests were updated in place (testid rename `active-execution-approve/reject` → `panel-approve/panel-reject`) — count unchanged. #GH-13 added 16 new across 2 files: RH-012..RH-018 in run-history.test.ts (11 covering lockedAt stamping via injected clock + getRunning string-array contract preserved + getRunningWithMetadata returns timestamps + re-mark preserves original lockedAt + releaseStaleLocks(0) clears every lock and returns released set + releaseStaleLocks(thresholdMs) preserves locks younger than cutoff + v1 file migration with `lockedAt: 0` sentinel + on-disk v2 rewrite verifies schema bump + on-disk v2 upgrade from v1 envelope + no-op write on empty store + persistence across re-init + negative-threshold clamp safeguard + future-lockedAt clock-skew preservation); WFR-CROSS-SESSION-LOCK in workflow-runner.test.ts (5 covering reject-when-lock-exists-for-same-ticket + allow-when-lock-is-for-different-ticket + allow-when-lock-is-for-different-project + message-wording-no-longer-claims-restart-clears-locks + sentinel-lockedAt=0-renders-as-"before the last app restart"). #GH-52 added 16 new across 5 files: WFR-036..040 in workflow-runner.test.ts (5 covering new phase markers + dedupe + awaitingApproval-closes-prior-step + full-11-phase sequence + `running` umbrella hidden); CMP-PROG-BREATH-001..003 in components-progress.test.tsx (3 covering `data-running` attribute); CMP-EXEC-LOG-AUTO-FOLLOW + USER-OPEN + AUTO-OFF in components-execution-log.test.tsx (3 covering auto-managed accordion that respects user-opened); DET-WIDGET-AWAIT-001/002 + DET-WIDGET-PROGRESS-001 in views-project-detail.test.tsx (3 covering inline Approve/Reject when awaiting + Cancel-restored when not + ProgressBar `data-running` flow-through); SKILL-MARKERS-001..003 in skill-markers.test.ts (3 static-analysis tests guarding skill ↔ runner phase-value drift). Plus updated RUN-SCHEMA-001 to enumerate the new 6 RunStates (20 total).

### E2E (Playwright)
| File | Tests | Covers |
|------|-------|--------|
| `tests/e2e/placeholder.spec.ts` | 1 | Runner is wired (1 + 1 === 2) |

**Total E2E:** 1 test (placeholder only). No Electron-driven E2E yet — deferred until first real user flow lands (#2+).

### Static checks
- `npm run lint` — ESLint 9 flat config (TypeScript + React)
- `npm run typecheck` — `tsc --noEmit` on both renderer and node tsconfigs
- `npm run build` — `electron-vite build` produces clean bundles

## Known Gaps
- **Electron-driven E2E**: No automated test launches the actual Electron app and exercises the IPC bridge through a real BrowserWindow + preload. Will be addressed when user flows exist (issue #2+).
- **Hot reload**: Cannot be programmatically asserted; developer must verify manually.
- **Packaging (`npm run dist`)**: Not run in CI yet. SCAFFOLD-004 verifies the `electron-builder.yml` config, but the actual installer build is manual.
- **Custom titlebar (#50)**: Unit tests cover the React component + IPC contract, but the *frameless* behaviour itself (drag region, Aero Snap, double-click-to-maximize, Alt+Space, macOS traffic-light alignment) can only be verified manually via `npm run dev` — no automated coverage of the actual `BrowserWindow` chrome.
- **Paperplane rebrand + userData migration (#GH-51)**: 9 migrate-userdata unit tests cover copy/idempotent/skip/marker/log/failed-mkdir paths against a temp dir. The end-to-end "old e-frank install upgraded to new Paperplane build retains projects + secrets" flow needs a manual installer-vs-installer smoke test against a Windows nsis upgrade — `safeStorage` ciphertexts in particular should be round-tripped on the upgrade path because the Windows DPAPI-bound key is account-scoped, not path-scoped.
- **Sticky phase headers (#GH-52 #8)**: jsdom doesn't compute layout, so `position: sticky` behavior under scroll can only be eyeballed in `npm run dev` (load a long Implementation phase, scroll past its header, verify the header pins). The CSS rule itself is in `ExecutionLog.module.css` and is exercised whenever a row renders — no automated coverage of the visual scroll behavior.
- **Inline approve/reject in widget (#GH-52 #3)**: unit tests cover button presence + IPC dispatch, but the live "actively-running run hits a checkpoint, widget surfaces buttons, click sends approval, run resumes" flow is currently only exercised manually. Pair with #50's "frameless titlebar can only be eyeballed" — both stack on the same gap (no Electron-driven E2E yet).
- **Global notification toast stack (#GH-59)**: unit coverage is comprehensive for store / single-toast / stack / dispatcher contract, but several outcomes are visual-only and only verifiable via `npm run dev`: (a) the bottom-right anchor stacks correctly above all routes (jsdom doesn't compute `position: fixed` + z-index against the real AppShell layout), (b) entry slide-and-fade + exit transitions read smoothly + `prefers-reduced-motion: reduce` flattens to opacity (jsdom doesn't compute CSS animations or the reduced-motion media query in tests), (c) approval-toast pulsing accent border (animation-driven), (d) pointer-events pass-through gaps between toasts so clicks reach underlying buttons, and (e) the cross-route live flow ("start run from Projects list, navigate to Connections, wait for approval marker → toast appears bottom-right; click Approve → toast dismisses and run resumes"). Stack the manual smoke against the existing #GH-52 inline-approve smoke and the #GH-57 sticky-head smoke — same `npm run dev` session can exercise all three.
- **GH-38 Skill management — discover + install + list**: 63 unit tests cover the three new backend modules + state hook + view + dialog. Behaviors that can only be verified manually with `npm run dev`: (a) real-filesystem scan of `~/.claude/skills/` (the InMemoryFs seam covers the parser + dedupe but not actual readdir + readFile against the user's `%USERPROFILE%\.claude\skills` tree on Windows / `~/.claude/skills` on macOS), (b) `npx skills add <ref> -g -y` actually shelling out and returning correct stdout/stderr (CI environment doesn't have npm-published `skills` CLI guaranteed), (c) `claude --dangerously-skip-permissions -p '/find-skills <query>'` streaming real Claude output into the FindSkillDialog and the heuristic `^[-*•]\s+name:` regex extracting candidates from whatever format `/find-skills` actually emits (output format isn't our contract — if `/find-skills` switches to JSON-only or unstructured prose, the install cards stop appearing and the user falls back to manual install input — that fallback IS unit-tested but the structured detection path needs eyeball), (d) `shell.openPath` from the row Open button actually launching the OS file manager at the skill folder (jsdom doesn't shell out), (e) FindSkillDialog stream auto-scroll keeping the latest line visible during a long find. Cancel-while-finding + cross-process restart (start two finds back-to-back, second waits for first's exit) are unit-tested via FakeSpawner state transitions, but the actual `SIGTERM` + 5-min timeout behavior on real `claude` processes is only verifiable manually. Sidebar "Skills" nav item appears between Connections and Settings — unit tests assert NAV_ITEMS order indirectly via testid presence but the visual nav-rail layout (icon + label spacing, hover state) needs eyeball.
- **GH-57 polish (panel/log visual tweaks)**: 5 of the 8 items are visual-only and need manual `npm run dev` verification — (#1) panel left/right edges align with ticket-list table edges; (#3) progress-bar sliding-gradient overlay reads as energy-flow while running, flattens on terminal; (#4b) accent bar 4px pill + softened halo + mask-faded ends on done/failed/cancelled; (#4c) sticky head solid bg + bottom-border boundary cue + extends past the row's left padding up to the accent bar; (#5) Refresh icon rotates around its own center with no horizontal/vertical drift. jsdom doesn't compute layout so `position: sticky`, `mask-image`, `scrollbar-gutter`, and CSS animations all need eyeball. The behavioral hooks (`data-running`, `data-visible`, testid renames, `scrollTo` invocation) are unit-tested; the visual outcome is not.
- **GH-67 Project Detail PRs tab — live GitHub fetch**: 10 unit tests in `tests/unit/views-project-detail-prs.test.tsx` (PRS-001..010) cover IPC round-trip, empty/populated DataTable, all 4 state badge variants (open→running, draft→neutral, merged→success, closed→warning), Refresh re-invocation, AUTH banner with Reconnect→navigation callback, RATE_LIMITED banner showing the message verbatim, row-click + per-row Open button both calling `shell.openExternal({url})`, and null-`reviewDecision` em-dash placeholder. 9 GithubClient tests added in `tests/unit/github-client.test.ts` (GH-CLIENT-PULL-001..006) covering the GraphQL POST shape (`first: 50, orderBy: UPDATED_AT DESC` + `Bearer` auth), successful node mapping, null-`author` tolerance for deleted accounts, AUTH error, BOTH rate-limit detection paths (REST-style 403 with `X-RateLimit-Remaining: 0` AND GraphQL `errors[].type === 'RATE_LIMITED'`), `repository: null` in GraphQL data → `NOT_FOUND`, token-never-leaked-in-error invariant, and invalid-slug short-circuit (no HTTP call). Behaviors verifiable only via `npm run dev` against a real GitHub Connection: (a) the GraphQL request actually succeeds against `https://api.github.com/graphql` with a real PAT and parses real responses (FakeHttpClient covers the contract but not the live endpoint), (b) the spinning IconRefresh animates smoothly during a live fetch (jsdom doesn't compute CSS animations), (c) clicking a row actually opens the PR in the user's default browser via `shell.openExternal` (jsdom asserts the IPC dispatch, not that the OS browser launched), (d) tab toggling Tickets→PRs→Tickets→PRs does NOT trigger a refetch (the `useProjectPulls` hook lives at the parent level so cache survives — verifiable via DevTools network panel against a real connection), (e) error banners (AUTH/RATE_LIMITED/other) layout above the DataTable with correct color tokens, (f) when the connection PAT lacks `repo` scope, the AUTH banner appears with correct copy and the Reconnect button routes to the Connections page. Stack the manual smoke with #GH-68's settings-tab smoke — same `npm run dev` session can exercise both.
- **GH-68 Project Settings tab**: 10 unit tests in `tests/unit/views-project-detail-settings.test.tsx` (SETTINGS-001..010) cover the tab structure, embedded AddProject prop wiring (mocked), Destructive Zone, delete confirm dialog name-match gating, IPC success + failure paths, active-run guard, and dialog Cancel. AddProject is mocked at module level to keep this surface narrow — its real connection-picker + validator behavior is tested by `tests/unit/views-add-project.test.tsx` (so a regression in the form itself surfaces there, not here). Behaviors that can only be verified manually via `npm run dev`: (a) the real embedded form renders with the project's current values pre-filled across all 4 AddProject FormSections (Basic Info, Source, Tickets, Workflow), (b) clicking AddProject's existing bottom "Save Changes" button actually round-trips through `projects:update` IPC and the success toast appears (live IPC path is unit-tested separately by `views-add-project.test.tsx`, but the new "wraps it inside a tab and dispatches toast on success" combination needs eyeball), (c) clicking AddProject's "Cancel" button reverts pending edits via the remount-key (key bump can't be observed in unit tests; manual smoke: edit a field, click Cancel, verify the field reverts to the project's stored value), (d) the Destructive Zone red panel reads as visually destructive (bordered red + danger-soft bg + IconAlert), (e) the typed-project-name confirm input rejects with a visually-clear disabled-state on the confirm button when the typed text doesn't match. Backend-side risks flagged in the GH-68 ticket but NOT enforced at the UI in this PR: repo path change during active run (backend `projects:update` currently doesn't refuse — flag as follow-up), connection re-binding mid-poll (backend `projects:update` doesn't bounce the per-project poller — flag as follow-up), delete-while-active is UI-level only (backend currently cancels-then-deletes; the disabled button + banner make the user-visible behavior consistent for as long as a run targets the project).
- **GH-65 app icon — paperplane brand**: 14 unit tests in `tests/unit/app-icon.test.ts` guard the 4 places the wiring needs to stay in sync (artifact existence + magic bytes; `electron-builder.yml` icon / win.icon / mac.icon / files entries; `src/main/index.ts` BrowserWindow `icon` prop resolved via `app.getAppPath()`; `scripts/build-icons.mjs` + `package.json` build:icons script). The actual packaged-app icon — Windows taskbar / installer / .exe in Explorer, macOS Dock / Finder / .app bundle — can ONLY be verified by running `npm run dist:win` / `npm run dist:mac` and visually inspecting the output. Playwright against a dev-built Electron doesn't show the packaged-app icon (only the BrowserWindow runtime icon, which on macOS is ignored entirely — Dock pulls from the .app's Info.plist + .icns). Stack the manual smoke with #GH-51's installer-vs-installer upgrade smoke — same `npm run dist:win` session covers both.
- **GH-72 PR A — per-run git worktrees (infra-only)**: 10 unit tests in `tests/unit/worktree-manager.test.ts` (WT-001..010) cover the add/remove/prune lifecycle with FakeSpawner argv assertions, error mapping (ADD_FAILED / REMOVE_FAILED / PRUNE_FAILED / IO_FAILURE / TIMEOUT), idempotent remove on missing dir, individual-failure-is-non-fatal in prune-sweep, and platform-aware path joining. 5 new workflow-runner tests (WFR-043..045) cover the integration: `addWorktree` called with right runId/baseBranch/repoPath; cwd handed to claude is the worktree path NOT the project's primary localPath; `removeWorktree` called on every terminal path (success / cancellation / failure); `addWorktree` failure → run state=failed without claude ever being spawned. Behaviors verifiable only via `npm run dev`: (a) real `git worktree add --detach` against a live repo creates the sibling checkout at the expected `<userData>/worktrees/<runId>` path and lands on the requested base branch, (b) the run actually performs git operations inside the worktree (commits land on a branch separate from the primary's HEAD), (c) `removeWorktree --force` cleans up even when the run made uncommitted changes (test exercises that path code-wise but real-fs cleanup needs eyeball), (d) `pruneStaleWorktrees` invoked at startup actually removes orphan dirs left by a crashed previous run (simulated in tests via FakeSpawner; real boot loop needs a crash-and-restart smoke), (e) the user's primary working tree is genuinely untouched while a run is in flight (manual: start a run, open the repo in another editor, observe no branch-switch or unstaged-change appearance). PR A still uses the single-active-run lock; concurrency is enabled by GH-72 PR B (issue follow-up).
- **GH-79 / GH-72 PR C — concurrent runs backend + plural IPC**: Backend Map migration in both `WorkflowRunner` (`active: ActiveRunCtx | null` → `Map<string, ActiveRunCtx>`) and `ClaudeProcessManager` (`active: ActiveRun | null` → `Map<string, ActiveRun>`); app-wide `ALREADY_RUNNING` dropped from both. WFR-003 split into 003a (different-ticket second start succeeds, listActive returns 2) and 003b (same-ticket second start still ALREADY_RUNNING via new in-memory per-ticket guard). CPM-007 flipped to assert concurrent runs return distinct runIds. New WFR-046a (listActive shape), WFR-046b (runs-changed event fires on start + terminal with current snapshot), WFR-047 (cross-project concurrency: 2 runs different projects coexist in active map, cancel one leaves the other alone). New plural IPC: `runs:list-active` channel + `runs:list-changed` event broadcast to renderer. New plural renderer hooks `useGlobalActiveRuns()` and `useActiveRuns(projectId)` in the existing state files alongside the legacy singular ones. Existing UI (Sidebar pill, ProjectList Status column, ProjectDetail ActiveExecutionPanel) still uses legacy singular hooks for back-compat — multi-run UI is PR D follow-up. Behaviors verifiable only via `npm run dev`: (a) starting a second ticket via the UI's Run button while ticket A is in flight actually succeeds end-to-end (test stops at runner-API contract; real-process spawn + worktree add + git ops needs eyeball), (b) cancelling run A via the active-execution panel doesn't surface a Cancel button for run B — but since UI shows only one run via legacy hook, this is mostly about verifying B continues to make progress even after A is cancelled, (c) `runHistory.json` durably holds multiple `running` lock entries per project (concurrent markRunning calls go through `enqueue` serialization which is already covered by RH-* tests, but the multi-lock-on-disk shape needs an eyeball), (d) renderer hook re-render storms with N>3 runs in flight: each `state-changed` transition triggers a `runs-changed` snapshot too, so plural subscribers see ~2× the events of singular subscribers — manual smoke for any UI built on top should watch for layout thrash. PR D's job: lift the UI surfaces from singular to plural so multiple in-flight runs are actually visible to the user.
- **GH-81 / GH-72 PR D — multi-run UI surfaces**: All 3 UI consumer call-sites migrated from legacy singular hooks (`useGlobalActiveRun`, `useActiveRun(projectId)`) to plural (`useGlobalActiveRuns`, `useActiveRuns(projectId)`); legacy singular hooks DELETED. Sidebar pill flips to multi-row layout when N>1 (`Active runs (N)` header + up to `SIDEBAR_MAX_VISIBLE_RUNS=3` rows + `+N more` overflow); single-row layout preserved for N=1 back-compat. ProjectList Status column gains count semantics: zero/one runs unchanged ("Running" / "Awaiting"), N>1 with any awaiting → "Awaiting (N)" (precedence over Running so user sees the actionable state count), N>1 with none awaiting → "Running (N)". New `<ActiveExecutionStack>` component in ProjectDetail: N=1 renders existing `<ActiveExecutionPanel>` inline (back-compat by feel); N>1 wraps in fixed-positioned stack overlay with compact header strip listing all runs + one expanded panel beneath. Header strip clicks change focused runId; focus state survives transitions on the focused run, falls back to first-of-array when focused run terminates. `state-active-run.test.tsx` rewritten as ACTIVE-RUNS-001..006 covering plural hook (listActive seed, projectId filter, multi-run rendering, list-changed listener updates, unsubscribe on unmount, missing-bridge degradation). 3 new LIST-MULTI tests in `views-project-list.test.tsx` covering the new badge semantics (Running(N) when N>1 no awaiting, Awaiting(N) precedence when any awaiting, no count suffix when N=1). `views-project-detail.test.tsx` + `views-project-detail-prs.test.tsx` mock invocations migrated from `useActiveRun mockReturnValue({run})` to `useActiveRuns mockReturnValue([{run}])`. New IPC fields propagated to test stubs in App.test.tsx + views-connections.test.tsx (`listActive`, `onListChanged`). Behaviors verifiable only via `npm run dev`: (a) start 2 runs across different projects — Sidebar pill renders the multi-row block with both project names + ticket keys, (b) start 3+ runs — Sidebar shows top 3 with `+N more` indicator visible, (c) start 2 runs on the SAME project — ProjectDetail's ActiveExecutionStack renders the compact header strip with both rows; clicking a row switches which panel is expanded; cancelling the focused run via the panel's Cancel button removes that row and the stack reflows, (d) ProjectList Status column shows "Running (2)" when 2 runs target one project; column updates live as runs start + finish, (e) the fixed-positioned stack overlay doesn't z-order conflict with other absolute/fixed elements (ApprovalPanel modal, toast stack, dropdown menus). The `data-active-run` page attribute on ProjectDetail still flips true/false based on first-of-N which is fine for the existing `--active-clearance` CSS custom prop (table-card bottom clearance).
- **GH-69 Foundation — Global Settings page (sidebar no-op fix + app-config store + page shell)**: 10 unit tests in `tests/unit/app-config-store.test.ts` (AC-STORE-001..010) cover the AppConfigStore lifecycle — init reads existing file / seeds defaults on missing file (no disk write until first set), FILE_CORRUPT on malformed JSON, UNSUPPORTED_SCHEMA_VERSION on wrong version, VALIDATION_FAILED on bad fields (with `details` carrying field-specific errors), set(partial) shallow-merges + persists, set(invalid) rejects without persisting, atomic write uses `.tmp-{uuid}` + rename, empty set({}) is a valid no-op, get/set before init returns IO_FAILURE not silent corruption. 5 hook tests in `tests/unit/state-app-config.test.tsx` (AC-HOOK-001..005) cover the renderer hook — mount calls appConfig.get; missing bridge degrades gracefully; get returning error sets the error field; update(partial) round-trips through appConfig.set; failed update preserves prior config and surfaces error. 5 page-shell tests in `tests/unit/views-settings.test.tsx` (SETTINGS-FOUND-001..005) cover the four section placeholder cards, the rail nav `#hash` hrefs pointing at matching DOM ids, the missing-bridge error banner, and the appConfig.get-error banner. New `appConfig: { get, set }` IpcApi stub propagated through 17 existing test files via a script-driven pass (NOT_USED_IN_FE_TESTS error stub so tests don't accidentally depend on appConfig behavior). Behaviors verifiable only via `npm run dev`: (a) clicking the sidebar Settings nav item actually opens the Settings page (no-op fallthrough at App.tsx is gone), (b) the page renders with a left rail + scrolling right content area; rail anchors scroll the corresponding section into view; (c) `<userData>/app-config.json` is created on disk on the first `set()` call (Foundation defers any writes; subsequent section PRs will trigger the first write when the user changes a Settings field), (d) the hide-on-hover scrollbar treatment on `.content` matches the ProjectDetail tabs treatment so it feels consistent. The 4 placeholder cards persist until each section's follow-up PR lands. Foundation does NOT touch `useTheme` — that migration is part of the Theme section follow-up.
- **GH-85 — Settings Claude CLI section (discovery + version + override)**: New `src/main/modules/claude-cli-probe.ts` does `where claude` (win32) / `which claude` (posix) discovery + `<binary> --version` capture via a small `runOnce(spawner, ...)` helper that accumulates stdout/stderr until exit and SIGTERM-kills on a 3s timeout. Validation gate distinguishes `PATH_NOT_FOUND` (`fs.access` failed), `NOT_EXECUTABLE` (`--version` non-zero / spawn-error / timeout), and `NOT_CLAUDE` (output doesn't match the loose `/claude/i` regex). Two new IPC channels: `claude-cli:probe` (uses `appConfig.claudeCliPath` if set, else PATH lookup) and `claude-cli:probe-override` (validates without persisting). `ClaudeProcessManager.RunRequest` gains optional `command?: string` for per-run binary override; the IPC `ClaudeRunRequest` deliberately does NOT mirror it so a renderer can't smuggle a custom command via the direct `claude:run` channel (drift guard test relaxed from `toEqualTypeOf` to `toMatchTypeOf` to lock the subset relationship). `WorkflowRunner` gains optional `appConfigAdapter` option; when provided (always in production main), reads `claudeCliPath` per run and threads it as the `command` override, so overrides take effect on the next run with no app restart. New `useClaudeCli()` hook in `src/renderer/state/claude-cli.ts` returns `{state: 'loading' | 'found' | 'not-found' | 'error', resolvedPath, version, source, error, refresh, testOverride, saveOverride, clearOverride}`; missing-bridge synchronously initializes to state='error'; saveOverride/clearOverride both call `appConfig.set` then re-probe so the resolved-path row reflects the change. New `<ClaudeCliSection>` in `Settings.tsx` renders three top-level states (Found with green check + path + version, NotFound with red exclamation + install-docs link via `shell.openExternal`, Error banner) plus Refresh button + override input row with Test→Save→Clear buttons gated by a `testedPath === overrideInput.trim()` invariant so the user can't persist a path they haven't validated. Tests: 20 in `claude-cli-probe.test.ts` (CLI-PROBE-RUNONCE-001..004 streaming/timeout/spawn-error + CLI-PROBE-DISCOVER-001..004 posix/win32/exit-nonzero/spawn-error + CLI-PROBE-VERSION-001..004 success/exit-nonzero/spawn-error/empty + CLI-PROBE-VALIDATE-001..005 covering all three error codes + ok path + CLI-PROBE-MAIN-001..003 orchestrator paths); 7 in `ipc-contract-claude-cli.test.ts` (IPC-CLI-001..004 channel strings + IpcApi shape + drift guards); 10 in `state-claude-cli.test.tsx` (CLI-HOOK-001..010 probe success path/override/not-found, override-with-null-version → not-found mapping, ok=false → error state, missing-bridge synchronous fallback, refresh re-probes, saveOverride + clearOverride both call appConfig.set + re-probe, testOverride does NOT persist); 6 in `views-settings.test.tsx` (SETTINGS-CLI-001..006 found-state rendering, not-found install-link opens external, Test+Save flow gating, NOT_CLAUDE validation error friendly message, Clear button enable/disable + IPC call, Refresh re-invokes probe); 2 in `claude-process-manager.test.ts` (CPM-OVERRIDE-001..002 per-run command override + fallback to constructor default); 3 in `workflow-runner.test.ts` (WFR-CLI-OVERRIDE-001..003 claudeCliPath set → CPM receives command override / null → no override / no adapter → no override); SETTINGS-FOUND-001 + SETTINGS-FOUND-004 updated to expect `settings-claude-cli-section` testid. `claudeCli: { probe, probeOverride }` stub propagated across ~19 test files via `tmp/inject-gh85-stubs.cjs` (NOT_USED_IN_FE_TESTS error stub). Behaviors verifiable only via `npm run dev`: (a) actually running `where claude` / `which claude` on a fresh Windows / macOS / Linux machine and verifying the discovered path matches `Get-Command claude` / `command -v claude` output, (b) `<binary> --version` actually exiting fast for the real `claude` CLI (the 3s probe timeout assumes it does — Anthropic could regress this), (c) the `--version` regex `/claude/i` actually matching whatever future Claude Code versions print (looseness is intentional but releases COULD ship something like "Code 2.0" with no "claude" substring, breaking the override gate; in that case the user would always see NOT_CLAUDE and have to clear the override or wait for an app update), (d) the per-run override actually takes effect on the next run without app restart (unit tests assert the runner calls CPM with the right `command` value; the real-process spawn against a custom binary needs an eyeball + actual ticket run), (e) the install-docs URL `https://docs.anthropic.com/en/docs/claude-code/quickstart` is still valid (rotates with Anthropic's docs site), (f) clicking the override-input field doesn't accidentally trigger autofill from password managers on macOS Safari Touch Bar / Windows credential helpers (no `autoComplete="off"` set yet — flag as follow-up if this surfaces).
- **GH-87 — Settings About section (version, build, platform, log dir, links)**: New `app:info` IPC handler returns a flat `AppInfoResponse` ( `appVersion` from `app.getVersion()`, `buildCommit` from `process.env.BUILD_COMMIT ?? 'dev'`, `platform` + `release` from `node:os`, `electronVersion`/`nodeVersion`/`chromeVersion` from `process.versions`). New `shell:open-log-directory` IPC opens `<userData>/runs/` via `shell.openPath` — narrower than extending `shell:open-path` allow-list. Build-time defines `__APP_VERSION__` + `__BUILD_COMMIT__` wired in `electron.vite.config.ts` for the renderer + mirrored in `vitest.config.ts` so unit tests resolve the symbols. New `useAppInfo()` hook in `src/renderer/state/app-info.ts` does a one-shot mount fetch, degrades to a build-time-defines fallback when bridge is missing or `app:info` returns `ok: false`. 4 hook tests in `tests/unit/state-app-info.test.tsx` (APP-INFO-001..004) cover ok=true, ok=false fallback+error, throwing IPC fallback, missing-bridge fallback. 5 page tests in `tests/unit/views-settings.test.tsx` (SETTINGS-ABOUT-001..005) cover all 7 diagnostic rows render, Open-log-dir calls `shell.openLogDirectory()`, Report-an-issue + Check-for-updates call `shell.openExternal` with the right GitHub URLs (`/issues/new`, `/releases`), and ok=false fallback path shows the error hint. SETTINGS-FOUND-001 + SETTINGS-FOUND-004 updated to expect `settings-about-section` testid (no longer a placeholder). `installApi` helper in views-settings.test.tsx extended to stub `app.info` + `shell.openLogDirectory` + `shell.openExternal` end-to-end. Behaviors verifiable only via `npm run dev`: (a) opening Settings → About renders the 7-row info grid populated from real Electron `process.versions` + real `node:os.release()`; (b) clicking Open-log-directory actually opens `<userData>/runs/` in the OS file manager (jsdom can assert the IPC dispatch but not that the OS launched a file-manager window); (c) clicking Report-an-issue / Check-for-updates actually opens the GitHub URLs in the default browser; (d) when run from a packaged build, `BUILD_COMMIT` is the real commit sha if the build was tagged at CI time (`dev` in dev builds is expected); (e) toast surfaces on `shell.openLogDirectory` failure (e.g. directory doesn't yet exist because no run ever happened — `shell.openPath` returns a non-empty string on failure which the main handler maps to `ok: false`).
- **GH-93 — Refactor Find Skill: direct skills.sh API + infinite scroll + installed dedupe**: Replaced the Claude-subprocess `/find-skills` pipeline with a direct HTTP call to `https://www.skills.sh/api/search?q=<encoded>&limit=<N>`. New `src/main/modules/skills-search-client.ts` wraps the existing `HttpClient` (FetchHttpClient/FakeHttpClient pattern from Jira/GitHub) with a sanitized error taxonomy (`NETWORK / TIMEOUT / RATE_LIMITED / SERVER_ERROR / INVALID_RESPONSE / INVALID_REQUEST`); query URL-encoded, accept-json header set, 10s timeout. IPC contract reshape: `SKILLS_FIND_START / SKILLS_FIND_CANCEL / SKILLS_FIND_OUTPUT / SKILLS_FIND_EXIT` channels + request/response/event types + `IpcApi.skills.findStart/findCancel/onFindOutput/onFindExit` all DELETED; one new channel `SKILLS_SEARCH: 'skills:search'` carries `{query, limit}` → `{skills: ApiSkill[], count: number}` with `ApiSkill = { id, skillId, name, installs, source }` mirroring the API response. Main process eagerly instantiates `SkillsSearchClient` at module load; the `claude` child process is never spawned for find-skills. FindSkillDialog rewritten as: search input with 300ms debounce + Search button (Enter bypasses debounce); paginated result list (name + source pill + installs count formatted as K/M); IntersectionObserver sentinel triggers a re-request with `limit += 20` on intersect, capped at 200 — the skills.sh API doesn't document `offset`, so paging is "re-request with bigger limit"; rows whose `skillId` matches the `installedIds` prop (folder basenames from `useSkills()`) render with an `Installed` badge + disabled install; install round-trip uses existing `skills.install` IPC with `ref = skillId`; out-of-order responses dropped via a request-sequence counter; reopening clears prior state. Reviewer-flagged W1 (install-error stickiness across searches) + W2 (sentinel was rendered inside `<ul>`, HTML5 forbids non-`<li>` children) folded in: `runSearch` now clears `installError` on every new request, and the sentinel `<div>` is rendered as a sibling of the `<ul>` rather than a child. Deleted files: `skill-finder.ts`, `skill-query-validator.ts`, `find-skill-cache.ts`, `find-skill-candidates.ts`, `skill-source-url.ts`, plus their 5 test files (`skill-finder.test.ts`, `skill-query-validator.test.ts`, `find-skill-candidates.test.ts`, `skill-source-url.test.ts`, `state-find-skill-cache.test.ts`). Tests: 18 in `skills-search-client.test.ts` (SEARCH-001..014 — URL shape + encoding + whitespace-trim + INVALID_REQUEST for empty/non-positive-limit + NETWORK/TIMEOUT/4xx/5xx mappings + non-JSON/missing-skills-array INVALID_RESPONSE + query-not-leaked-in-error invariant across all error paths + malformed entry filtering + count fallback to array length + custom-baseUrl injection); 12 in `ipc-contract-skills.test.ts` (IPC-SKILLS-S-001..005 — channel string + IpcApi.skills.search signature + payload shapes + drift guards confirming SKILLS_FIND_* are deleted + regression that list/install/remove are still present); 15 in `components-find-skill-dialog.test.tsx` rewritten (DIALOG-001..003 basic render + DIALOG-SUBMIT-001/002 search dispatch + DIALOG-INSTALLED-001 dedupe + DIALOG-INSTALL-001/002 install + DIALOG-ERROR-001 banner + DIALOG-PAGING-001..003 sentinel intersect + cap + exhaustion + DIALOG-EMPTY-001 + DIALOG-RESET-001 reopen wipes state + DIALOG-W1-001 install-error clears on new search). `views-skills.test.tsx` trimmed: VIEW-SKILLS-001..007 retained (page render, empty state, CTA prefill, table rows, open action, refresh, error banner); the 3 VIEW-SKILLS-CACHE tests removed because the find-skill-cache module is gone. Test stubs updated across 15 consumer test files to replace the 4-line `findStart / findCancel / onFindOutput / onFindExit` block with a single `search` stub. Behaviors verifiable only via `npm run dev` against live skills.sh: (a) the real `https://www.skills.sh/api/search` endpoint returns the documented `{ query, searchType, skills, count, duration_ms }` shape (FakeHttpClient covers our parsing contract but not the live response shape); (b) IntersectionObserver actually fires on scroll-to-bottom in the real renderer — jsdom can't compute layout so we stub the observer in tests; manual smoke: scroll a 50+ result query to the bottom + verify the next page lands seamlessly; (c) the 200-result cap actually stops paging in the UI before the user notices the API stops being useful; (d) the install round-trip from a search result actually `npx skills add <skillId>` shells out and lands a folder under `~/.claude/skills/<skillId>/` (unit-tested for the IPC dispatch; the real install path is covered by GH-38's existing manual smokes); (e) Network-error UX: temporarily DNS-block `www.skills.sh` + verify the NETWORK banner appears without spinner-stuck states; (f) Rate-limit UX: skills.sh aggressive enough usage gets a 429 — the RATE_LIMITED banner copy needs eyeball; (g) the previously-required `claude` CLI is no longer required to use the Find Skill dialog (a fresh machine without Claude installed should still be able to install skills); (h) the `Installed` badge actually appears for already-installed skills when re-opening the dialog right after an install (depends on `useSkills.refresh()` propagating quickly through the parent — the unit tests confirm `onInstalled` fires; the visual re-render needs eyeball). Pre-existing `tests/unit/views-settings.test.tsx > SETTINGS-FOUND-004` failure on main is unchanged by this PR (looks for `settings-placeholder-defaults` after GH-86 replaced it with the real `settings-defaults-section` — flag as follow-up). **Worktree caveat (#GH-93 process note):** the original ef-auto-feature worktree at `<userData>/worktrees/...` was destroyed by a code-review subagent's `git worktree remove` partway through Phase 6 (the agent claimed to extract files via `git cat-file` from the now-empty worktree, but in reality the uncommitted changes were lost). All files in this PR were reconstructed from the conversation transcript directly into the primary `D:/e-frank` worktree before commit. Future ef-auto-feature runs should commit aggressively after Phase 4 to avoid this class of data loss, or the review subagent should be sandboxed to a read-only seam.
- **GH-66 — Project Detail past-run detail view (unblock Runs-tab click-through)**: New `runs:get` IPC channel + `IpcApi.runs.get` round-trips to the existing `RunStore.get(runId)` (which already returns `{ ok: true, data: Run } | { ok: false, error: { code: 'NOT_FOUND' | 'CORRUPT' | 'UNSUPPORTED_SCHEMA_VERSION' | 'IO_FAILURE', ... } }`); no schema or storage changes — only an IPC surface around an existing read. `ExecutionView` resolution forks: live runs still resolve via `runs.current()`, past-run lookups fall back to `runs.get`, NOT_FOUND surfaces the existing not-found UI with refreshed copy ("This run no longer exists — its history record may have been deleted."). New `isLive` discriminator on the ready-state lets the view render an `Interrupted` warning badge for sidecars persisted as `running`/`pending` whose runId isn't claimed by the runner (crash-orphaned snapshots), suppresses the Cancel button + ApprovalPanel actions on past-run lookups (no live process to receive them), and toggles a meta row beneath the subtitle showing branch (from `Run.branchName`), duration (computed from `startedAt → finishedAt`), and a PR link (from `Run.prUrl`, opens via `shell.openExternal`). Back-button routing gains a new optional `onBackToRuns?: () => void` prop on `ExecutionView` — past-run clicks use it, live runs fall back to `onBack`. `App.tsx` extends `detail` ViewState with `initialTab?: TabId`, `ProjectDetail` accepts `initialTab` and seeds `useState<TabId>(initialTab ?? 'tickets')`, and `renderExecution` plumbs `{ kind: 'detail', projectId, initialTab: 'runs' }` so the user lands back on the Runs DataTable they clicked from. Tests: 3 new IPC-contract tests in `tests/unit/ipc-contract-runs.test.ts` (IPC-RUNS-GET-001..003 — channel string + IpcApi shape + signature); 6 new ExecutionView tests in `tests/unit/views-execution-view.test.tsx` (EXEC-PAST-001 terminal run renders with branch/duration/PR link; EXEC-PAST-002 NOT_FOUND surfaces the not-found UI; EXEC-PAST-003 stale-running snapshot renders Interrupted badge + hides Cancel; EXEC-PAST-004 PR-link click dispatches `shell.openExternal` with the right URL; EXEC-PAST-005 Back from past-run calls `onBackToRuns`; EXEC-PAST-006 Back from live run calls `onBack` not `onBackToRuns`); 2 new ProjectDetail tests in `tests/unit/views-project-detail.test.tsx` (DET-INITIAL-TAB-001 initialTab='runs' seeds the Runs tab without a click; DET-INITIAL-TAB-002 omitted initialTab defaults to Tickets). `runs.get` stub propagated across 17 consumer test files (`tests/unit/{App,components-add-connection-dialog,components-approval-panel,dialog-folder-picker,state-active-run,state-connection-resources,state-connections,state-notification-dispatchers,state-run-log,state-skills,views-add-project,views-connections,views-execution-view,views-project-detail,views-project-detail-prs,views-project-detail-settings,views-skills}.test.tsx`) via a small targeted insert + manual fix for the two files where the regex landed inside a wrapped declaration. Behaviors verifiable only via `npm run dev`: (a) clicking a Runs-tab row navigates to the new RunDetail view (the unit test asserts the IPC + render result, but the actual route transition from a real ProjectDetail click against a real ExecutionView mount needs eyeball); (b) the meta row reads correctly against a real terminal Run sidecar (jsdom doesn't compute the meta-row CSS layout — wrap behaviour with long branch names + a real-world PR URL needs eyeball); (c) Interrupted badge actually appears for a crash-orphaned run (simulating this requires force-quitting the app mid-pipeline + reopening, which is manual by definition); (d) the empty-state when the log NDJSON file has been rotated/deleted but the sidecar JSON remains (`useRunLog` already swallows readLog failures and leaves the timeline as the snapshot's `steps`, so it gracefully shows the recorded steps without per-step logs — but the user-facing emptiness needs eyeball to confirm it reads as "log no longer available" rather than "broken"); (e) PR-link button actually opens the GitHub PR in the default browser via `shell.openExternal` (unit tests assert IPC dispatch); (f) Back navigation from RunDetail genuinely lands on the Runs tab with the row the user clicked still visible (the initialTab plumbing is unit-tested, but real-world scroll-position preservation is NOT — the ticket flagged scroll preservation as "if cheap" and this PR didn't implement it, so the user lands at the top of the list); (g) very long persisted logs (multi-MB) actually load and render in the past-run timeline (`runs.readLog` returns all entries at once today; the ticket flagged head-truncation as a follow-up not implemented here — long-run smoke needs eyeball for jank).
- **GH-95 — Workflow runner timeout: bump default + reclassify timeout-after-PR as success**: Two-part change in a single PR. **Change A — default timeout 30 → 60 min:** `DEFAULT_TIMEOUT_MS` in `src/main/modules/claude-process-manager.ts` raised to `60 * 60 * 1000`; `DEFAULT_APP_CONFIG.defaultRunTimeoutMin` in `src/shared/schema/app-config.ts` raised to `60`; `acceptance/claude-process-manager.md` doc updated; 5 test fixture files (`tests/unit/{views-settings,state-app-config,state-claude-cli,state-theme,views-add-project}.test.tsx`) updated `30 → 60` in their hard-coded `defaultRunTimeoutMin` fixtures so the default-aligned tests still match. **Change B — timeout-after-PR reclassified as completed-with-warning:** new optional `terminalWarning?: string | null` field on `Run` (`src/shared/schema/run.ts`). In `WorkflowRunner.runClaudeWithApprovals`, between the `cancelled`-reason branch and the existing non-completed-reason throw, a new branch checks `exitEvent.reason === 'timeout' && ctx.run.prUrl` — when both are true the runner stamps `terminalWarning` with a human-readable message ("claude exited with reason=\"timeout\" after PR creation; tail work (ticket update / memory-bank refresh) may not have completed") and `return`s, so the pipeline lands on the success path with `state='done'`. The `ApprovalAbandonedError` check below is intentionally bypassed — by the time `creatingPr` has stamped `prUrl`, the planning approval marker has already been resolved. UI: `<ExecutionView>` renders a yellow advisory chip beneath the title (`<div data-testid="execution-terminal-warning">`) when `Run.terminalWarning` is non-null; the existing meta row (branch / duration / PR link) remains intact below it so the user can still open the merged PR. `ExecutionView.module.css` adds a `.terminalWarning` rule using soft-yellow palette (`--bg-warning-soft`, `--border-warning`, `--text-warning`) consistent with the existing `Interrupted` warning treatment. Tests: 2 new in `workflow-runner.test.ts` (WFR-TIMEOUT-AFTER-PR-001 — emit `committing` → `pushing` → `creatingPr` with prUrl payload then timeout, asserts final `state='done' status='done' prUrl=set terminalWarning` matches /timeout/i + /tail/i; WFR-TIMEOUT-BEFORE-PR-001 — timeout with no prUrl set still goes to `state='failed' status='failed' error~/timeout/i`, regression guard for the existing failure path); 2 new in `views-execution-view.test.tsx` (EXEC-TERMINAL-WARNING-001 — past-run with terminalWarning + prUrl renders chip + Done badge + PR link; EXEC-TERMINAL-WARNING-002 — past-run without terminalWarning does NOT render the chip). Run total: 65 workflow-runner tests passing (+2 new), 30 settings tests passing (1 SETTINGS-DEFAULTS-006 expectation updated 30→60 to match the new makeConfig default), full unit suite 1311+/1324 (the 1 failure is `claude-cli-probe.test.ts > CLI-PROBE-VALIDATE-003` flake in this worktree under parallel load — passes when re-run alone, unrelated to this PR). Behaviors verifiable only via `npm run dev` / a real autonomous run: (a) starting a long autonomous run that organically blows past 30-min and lands in the new 60-min budget — verify the `creatingPr` marker arrives, then if the tail (ticket update) takes longer than the remaining budget the timeout fires AND the run lands in `done` with the yellow chip rather than `failed`; (b) the chip's color reads as advisory (warning palette) rather than error against both light and dark themes — jsdom doesn't compute CSS custom-prop fallbacks; (c) the chip wraps gracefully on narrow viewports without obscuring the PR-link button below it (jsdom doesn't compute layout); (d) the chip persists when the user navigates away from the live ExecutionView and clicks back into the past-run via the Runs tab — the test EXEC-TERMINAL-WARNING-001 covers the past-run resolution, but the live→past transition needs eyeball; (e) the existing failure semantics for non-PR-creating timeouts (run killed mid-implementation) still surface as the red Failed banner, NOT a yellow chip — the WFR-TIMEOUT-BEFORE-PR-001 unit test asserts the runner-side state, but the renderer-side rendering of `state='failed'` runs needs eyeball confirmation it didn't accidentally pick up the chip styling.
- **GH-84 — Settings Theme section (light/dark/system + matchMedia + flash prevention)**: `state/theme.ts` rewritten as 3-mode + app-config-backed; `localStorage['ef.theme']` repurposed as a **write-through cache for the index.html bootstrap script** (always stores the RESOLVED concrete value, never literal `'system'` — so the bootstrap's binary check works unchanged). Hook return gains `resolvedTheme: ResolvedTheme` (system mode resolves to `'light' | 'dark'` via `window.matchMedia('(prefers-color-scheme: dark)')`), `loading` (true while `useAppConfig` loads), and `setTheme(next: ThemeMode)` (async, returns after persist). `toggle()` escapes system mode by flipping to opposite-of-`resolvedTheme`. matchMedia listener active only when preference is `'system'`; uses modern `addEventListener` with legacy `addListener` fallback for older Safari. One-time legacy migration: on mount where `app-config.theme === 'dark'` (default) AND localStorage has `'light'`, promotes via `update({ theme: 'light' })` — idempotent after first successful migration. `state-theme.test.tsx` rewritten to 13 tests (THEME-001..013) covering all three modes, matchMedia listener wire-up, live OS-change reaction, setTheme + toggle paths from each starting mode, the legacy migration, the no-fire-on-non-default case, and optimistic local update when persist fails. `components-theme-toggle.test.tsx` rewritten to 8 tests (CMP-THEME-001..008) — mocks `useTheme` directly so the component tests focus purely on the UI (icon choice from resolvedTheme, aria-label, click→toggle dispatch). `views-settings.test.tsx` updated with 4 new SETTINGS-THEME tests (RadioCardGroup renders 3 options, click calls setTheme with right value, current value is aria-checked, loading hint visible when hook is loading); existing SETTINGS-FOUND-001 updated to assert `settings-theme-section` testid replaces the legacy `settings-placeholder-theme`. Behaviors verifiable only via `npm run dev`: (a) opening Settings → Theme renders the 3-card RadioCardGroup and selecting a card persists immediately; (b) selecting `system` while OS is in dark mode → UI flips to dark, then change OS to light → UI flips to light without user action (matchMedia change event); (c) cold-boot after selecting any non-default theme → no theme flash on first paint (bootstrap script reads `localStorage['ef.theme']` cache before React mounts); (d) cold-boot with a pre-existing `localStorage['ef.theme']='light'` and a fresh app-config → useTheme migration runs once, app-config.theme = `'light'`, subsequent boots no longer migrate (idempotent); (e) Sidebar ThemeToggle still works as a one-click light↔dark quick-swap (correctly escapes system mode by flipping to opposite-of-resolved).

## Bugs Discovered
None during #1 or #2 implementation. (#1 had a critical preload-path bug caught by the reviewer pre-merge, plus the sandbox + ESM incompatibility caught by manual `npm run dev` — both fixed and guarded.)
