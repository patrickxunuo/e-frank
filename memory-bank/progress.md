# Progress

## Current Sprint / Focus
Phase 3.5 — most done. #24 Connection model + Connections view shipped (paste-token auth + UI polish). #25 Project ↔ Connections refactor + picker UI shipped (schema break, JiraPoller-via-ConnectionStore, AddProject picker UI, getReferencingProjectIds wired). Phase 4 (#10–#13) unblocked. Remaining Phase 3.5: #26 (GitHub Device Flow) and #27 (Atlassian OAuth 3LO loopback) — both upgrade individual provider auth methods and can land before or after Phase 4 features.

## Log
<!-- Newest entries first. Format: - YYYY-MM-DDTHH:MMZ [status] feature-name (developer) — notes -->
<!-- ALWAYS use UTC time (Z suffix). Run: date -u +"%Y-%m-%dT%H:%MZ" -->
- 2026-05-13T15:02Z [DONE] #GH-63 Find Skill follow-ups from #62 shipped via /ef-auto-feature (PR #64 on `feat/GH-63-find-skill-followups`). Three bugs, one bundled PR. (1) Bug 1 — verbose query "find a skill that can create structual jira ticket and github issue" surfaced find-*-named skills. `stripQueryFiller` was verified to clean the wrapper correctly (manual repl run: `"create structual jira ticket and github issue"`), so the bug was downstream: `buildFindSkillsPrompt` framed the cleaned query as a "Search for ... matching" keyword, encouraging Claude to keyword-match on residual meta-words. Reworked the prompt to position the cleaned query as a task description ("The user wants help with this task: …") with explicit forbid: `do NOT match on the word "find" or other meta-words about searching for skills`. Steps now tell Claude to extract verbs+nouns and rank by underlying-task fit, not by literal name matching. (2) Bug 2 — close+reopen erased cached results. The existing `FindSkillDialog` cache hydration is correct (DIALOG-FIND-CACHE-001 still passes); the user's symptom was most likely a stale dev build. Fortified regression surface with VIEW-SKILLS-CACHE-001 driving the full close+reopen path through the actual `Skills.tsx` parent (Find Skill button → search → Esc → Find Skill button → assert card present + only one findStart called). If a future refactor changed the reopen-prefill behavior to non-empty default, this test would catch the cache wipe. (3) Bug 3 — new requirement: clear cache on Skills page exit. Added `useEffect(() => () => clearFindSkillCache(), [])` in `Skills.tsx`. StrictMode-safe: cache is empty on first mount so the dev double-invoke is a no-op. VIEW-SKILLS-CACHE-002/003 cover unmount-wipes-store + remount-shows-empty-hint (with the literal "Search asks Claude" text assertion matching the spec's "navigating back shows the empty hint, not stale results"). Tests: 1161/1173 (was 1142; +4 new — FIND-PROMPT-003 frames-as-task-description + asserts no "Search for ... matching" framing as regression guard, FIND-PROMPT-004 verbose-query-cleaned-input + meta-word-forbid regex, VIEW-SKILLS-CACHE-001 close+reopen-via-parent-button preserves cache, VIEW-SKILLS-CACHE-002 unmount-wipes-store, VIEW-SKILLS-CACHE-003 remount-shows-empty-hint). Reviewer verdict: Ready for PR; two nice-to-haves applied (header comment `00X` → `004`; CACHE-003 added explicit empty-hint text assertion). Lint + typecheck + build clean.
- 2026-05-13T11:43Z [DONE] #GH-38 FindSkillDialog result-cache + Clear button shipped via /ef-plan + manual implement (bundled into same PR #62 on `feat/GH-38-skill-management`, follow-up to the 5-feature polish bundle at 11:15Z). Three small features. (1) `src/renderer/state/find-skill-cache.ts` — singleton module-level store holding `{ query, lines, findError, nextLineId }`. Memory-only (no localStorage / userData), drops at app close. Deliberately no `useSyncExternalStore` — only the dialog reads/writes, only one dialog instance is mounted at a time, so synchronous get/save/clear via direct imports is simpler than a reactive subscription. `OutputLine` interface lifted into the cache file as the single source of truth. (2) FindSkillDialog hydrate-on-open + write-on-exit wiring — the reset-on-open `useEffect` now hydrates from the cache (initialQuery prop wins when it's non-empty AND differs from cached.query, so the EmptyState CTA's `ef-feature` prefill still triggers a fresh search). `handleStartFind` resets local lines (loader shows) but does NOT clear the cache; cache clears in the first-line callback so a Stop-before-output cancels back to the previously-cached candidates (handled in `onFindExit` via a `gotFirstLineRef` flag — if true, save the new lines to cache; if false, restore local state from the still-intact cache). `inflightQueryRef` captures the query at find-start so the cache save records the right string regardless of input edits during the find. (3) Clear button in `.candidatesHead` (right-aligned, ghost size-sm, `find-skill-clear` testid) wipes both the cache and the local view. `.candidatesHead` becomes a flex row with `space-between`; new `.candidatesLabel` class carries the original mono-uppercase eyebrow styling. Button is hidden when there are no candidates (the candidates section itself doesn't render) and disabled while `isFinding` is true. Tests: 1142/1154 (was 1129; +13 net new across 2 files — CACHE-001..005 in new `state-find-skill-cache.test.ts` covering initial state + round-trip + clear + has-flag + reset-helper; DIALOG-FIND-CACHE-001..004 in `components-find-skill-dialog.test.tsx` covering close+reopen-preserves / new-search-clears-only-after-first-line / initialQuery-prop-overrides-cache / Stop-without-output-restores-cache; DIALOG-FIND-CLEAR-001..004 covering button-wipes-everything / hidden-when-empty / hidden-during-find / wipe-survives-close+reopen). Lint + typecheck + build clean. Visual smoke flagged for manual `npm run dev`: verify the cards stay visible after dialog close+reopen, verify Clear button placement next to "Recommended skills" label, verify Stop-before-output restores prior cards.
- 2026-05-13T11:35Z [PLAN] #GH-38 FindSkillDialog result-cache + Clear button planned via /ef-plan: 3 features bundled on `feat/GH-38-skill-management` as a follow-up to the 5-feature polish bundle. (1) `find-skill-cache` module store mirroring the #GH-59 notifications-store pattern but without useSyncExternalStore (single consumer, synchronous hydration on open transition suffices). State: query + raw stream lines + findError + nextLineId. Memory-only — drops at app close. (2) Hydrate-on-open + write-on-exit wiring in FindSkillDialog: rework the existing reset-on-open useEffect to hydrate from cache (initialQuery prop wins when non-empty + differs from cached.query); save to cache in the onFindExit handler when lines were produced; the key "Stop preserves previous candidates" behavior is implemented as "defer cache clear until first streamed line of the new find" — if user cancels before any output, cache stays intact and renderer rehydrates from it on the cancelled-exit. (3) Clear button in `.candidatesHead` wipes cache + local view, hidden when nothing to clear, disabled while finding. ~13 new tests across CACHE-001..005, DIALOG-FIND-CACHE-001..004, DIALOG-FIND-CLEAR-001..004.
- 2026-05-13T11:15Z [DONE] #GH-38 Skill management polish + remove-skill bug fix shipped via /ef-plan + manual implement (bundled into the open PR #62 on `feat/GH-38-skill-management`). Five features across one PR. (1) Critical remove-skill bug fix: `uninstallSkillViaNpx` was spawning `npx skills remove <ref> -g` without `-y`, so npx waited on an interactive "Are you sure?" prompt and the child hung until the 5-minute timeout fired — the user perceived this as "Remove runs forever". Added `-y` to the default flags + a defensive try/finally around `handleConfirmRemove` in `Skills.tsx` so the "Removing…" state always clears even if the IPC throws. (2) Install/uninstall success toasts: `useSkills().remove()` dispatches `Removed <name>` toast on success via the existing #GH-59 notifications store; `FindSkillDialog`'s `handleInstall` dispatches `Installed <name>` on success. Failure paths intentionally don't toast — inline error banner stays the place for diagnostics. (3) `getSkillSourceUrl(ref)` utility in `src/renderer/components/skill-source-url.ts` derives `https://github.com/{owner}/{repo}` from `owner/repo@skill` and `owner/repo` refs; returns null for bare-name refs. New IPC channel `shell:open-external` with main-side host allow-list (`github.com`, `gitlab.com`, `bitbucket.org`, `npmjs.com`, `www.npmjs.com`) in `src/main/modules/shell-external-validator.ts` — rejects `javascript:`/`file://`/look-alike hosts so a compromised renderer can't shell-open arbitrary URLs. New `IconExternal` glyph in `icons.tsx`. (4) One-skill-per-row card list: `.candidateGrid` replaced with `.candidateList` (flex column), `.card` becomes `grid-template-columns: minmax(0,1fr) auto` with a `.cardBody` left + `.cardActions` right cluster (View + Install). Wider rows let descriptions breathe. New `find-skill-view-{ref}` testid; `data-layout="row"` attribute on the list for regression assertion. (5) Sticky search-top + sticky install-bottom + scrollable middle: `.body` becomes `display: grid; grid-template-rows: auto 1fr auto; max-height: 70vh`. New `.bodyHeader` / `.bodyScroll` / `.bodyFooter` slots with `find-skill-body-{header,scroll,footer}` testids. Long candidate lists scroll inside `.bodyScroll` while the search row and "Install by name" block pin in view. Tests: 1129/1141 (was 1118; +23 net new across 5 files — NPX-UNINSTALL-001 updated to assert `-y` flag (regression guard against the original hang), SKILL-URL-001..006 in new `skill-source-url.test.ts`, EXT-URL-001..007 in new `shell-external-validator.test.ts` covering scheme rejection + host allow-list + look-alike host rejection, DIALOG-FIND-LAYOUT-001 + DIALOG-FIND-CARD-LAYOUT-001 + DIALOG-FIND-CARD-VIEW-001..003 + DIALOG-FIND-TOAST-001..002 in `components-find-skill-dialog.test.tsx`, HOOK-SKILLS-REMOVE-TOAST-001..003 in `state-skills.test.tsx`). 8 other test files updated to add `openExternal: vi.fn()` to their `shell` stubs (typed branches). Lint + typecheck + build clean.
- 2026-05-13T10:45Z [PLAN] #GH-38 Skill management polish + remove-skill bug fix planned via /ef-plan: 5 features bundled on `feat/GH-38-skill-management` (single PR per project default). (1) Remove-skill `-y` flag fix — root cause of "Remove runs forever" is `uninstallSkillViaNpx` defaulting flags to `['-g']` while `installSkillViaNpx` uses `['-g', '-y']`; without `-y` npx waits on an interactive "Are you sure?" prompt and the child hangs (5-min timeout in the wild). Add the flag, parity with install, plus a try/finally around `handleConfirmRemove` so the "Removing…" state always clears. (2) Success toasts via #GH-59 `dispatchToast` for both install and uninstall (`Installed <name>` / `Removed <name>`, 4s ttl, dedupeKey scoped per ref); failure paths keep the existing inline error banner. (3) View-source button on candidate cards — new `getSkillSourceUrl(ref)` pure utility maps `owner/repo@skill` → `https://github.com/owner/repo`, bare names → null. New `shell:open-external` IPC with host allow-list (github/gitlab/bitbucket/npmjs) in main, `IconExternal` glyph in `icons.tsx`. (4) Single-column candidate list — drop the 2-col grid, give each row horizontal layout with a left body + right action cluster (View + Install). (5) Sticky layout — `.body` becomes a 3-row grid (`grid-template-rows: auto 1fr auto; max-height: 70vh`) so search pins top and "Install by name" pins bottom; long candidate lists scroll inside the middle slot.
- 2026-05-11T22:37Z [DONE] #GH-38 Skill management — discover, install, list shipped via /ef-auto-feature. PR #62. Single bundled PR across three lanes. (A) Backend: three new modules under `src/main/modules/` — `skills-scanner.ts` reads `~/.claude/skills/*/SKILL.md` + optional `<cwd>/.claude/skills/*/SKILL.md` via an injected `SkillsScannerFs` seam (mirroring skill-installer's `SkillInstallerFs`), parses `--- key: value ---` frontmatter with hand-rolled tolerance for CRLF and quote-wrapped values, dedupes by id (project source overrides user), sorts case-insensitively by name; `skill-npx-installer.ts` wraps `Spawner` to run `npx skills add <ref> -g -y` with `SKILL_REF_REGEX = /^[a-zA-Z0-9@][\w./@-]+\$/` rejecting all shell metacharacters before reaching `spawner.spawn()`, tails stdout+stderr to 4KB each, treats non-zero exit as `status:failed`, 5-minute hard timeout SIGTERM-kills the child; `skill-finder.ts` runs `claude --dangerously-skip-permissions -p '/find-skills <query>'` in a single-active-finder lifecycle (deliberately separate from ClaudeProcessManager so a workflow run and a find can coexist), line-buffers stdout/stderr, emits Output + Exit events with reason completed/cancelled/error. Plus `skill-query-validator.ts` — extracted in-PR after code review caught a shell-injection vector in `query` (NodeSpawner defaults to `shell:true`, concatenates argv into a cmd.exe `/c` line, a `query` of `q & calc.exe` would have escaped). `isValidFindSkillsQuery()` whitelist accepts plain English (alphanumerics + spaces + `.,/@:'?!-`) and rejects every cmd.exe / sh separator/substitution char, 200-char length cap. (B) IPC: extend `shared/ipc.ts` + `preload/index.ts` + `main/index.ts` with `skills:list`, `skills:install`, `skills:find-start` + `skills:find-output` (event) + `skills:find-exit` (event) + `skills:find-cancel`, plus companion `shell:open-path`. The shell:open-path handler enforces an allow-list at the boundary (absolute path required, must contain `.claude/skills/` segment, must not have `..` traversal) — a compromised renderer cannot ask Electron to launch arbitrary `.exe`/`.app` files. (C) Renderer: `state/skills.ts` `useSkills()` mirrors `useConnections` (window.api null-tolerant fallback to IPC-bridge-unavailable error); `views/Skills.tsx` page with DataTable (Name · Description · Source · Open action), Source Badge variant `success` for project / `neutral` for user, EmptyState CTA opens FindSkillDialog pre-filled `ef-feature`; `components/FindSkillDialog.tsx` query input + monospace streamed output area (auto-scrolls ONLY when user is pinned to bottom — 8px epsilon — so reading earlier candidates doesn't yank back) + candidate cards extracted from streamed lines via `^[\s]*[-*•]\s+([a-z][a-z0-9-]+(?::[a-z][a-z0-9-]+)?)\s*[:—-]\s*(.+)\$` regex with Install button per candidate + manual-install input fallback when regex misses + Stop button replaces Search while finding (dispatches findCancel) + non-zero exit code surfaces error banner (separate from `reason: 'error'` spawn-failure case) + dialog-close-while-finding cancels; `IconSkills` 4-point sparkle in icons.tsx; Sidebar extends SidebarNavId with 'skills' between Connections and Settings; App.tsx adds `{kind:'skills'}` ViewState variant + route handler + render; AppShellRoute union extended. 1079/1079 unit tests pass + 12 pre-existing skipped (was 1005 baseline at #GH-59; +74 new across 7 files — SCAN-001..008 + SCAN-PARSE-001..006 frontmatter + scan edge cases; NPX-INSTALL-001..020 ref-regex matrix + argv + exit-code mapping + tail clamp + timeout; FIND-001..031 line-buffer + lifecycle + cancel + spawn-error + idle-after-exit; FIND-QUERY-001..011 shell-injection whitelist (added in-PR after review); HOOK-SKILLS-001..005 useSkills loading/error/refresh; VIEW-SKILLS-001..007 page render + empty CTA + table + Open IPC + Refresh + error; DIALOG-FIND-001..010 streaming + candidate extraction + install + manual fallback + cancel + close-while-finding). Reviewer verdict: Fix critical issues first → fixed in-PR (1. shell-injection via `query` parameter at `skill-finder.ts:123` now hard-rejected by `isValidFindSkillsQuery` at the IPC boundary with 11 dedicated tests; 2. `shell:open-path` accept-anything → now requires `.claude/skills/` segment + no `..` + absolute path); plus 4 warnings landed (3. auto-scroll only when pinned-to-bottom; 4. non-zero exit code surfaces error banner separately from `reason: 'error'`; 5. dead `--accent-border, var(--border-default)` CSS fallback dropped). Remaining suggestions (file-naming mix `skills-scanner` vs `skill-finder` vs `skill-npx-installer`, empty-state CTA opens dialog vs direct-install, CANDIDATE_RE lowercase-only, `useMemo` over `lines` is O(n²) over session) explicitly accepted as acceptable trade-offs / future polish. Manual smoke flagged in testBaseline.md: real ~/.claude/skills/ fs scan on Windows/macOS, actual `npx skills add` invocation (CI doesn't guarantee `skills` CLI present), `/find-skills` streaming + heuristic detection against actual Claude output format, `shell.openPath` launching OS file manager, auto-scroll gesture, cross-process restart of finds.
- 2026-05-11T22:02Z [PLAN] #GH-38 Skill management — discover, install, list planned: single bundled PR across three lanes. (A) Backend: 3 new modules — `skills-scanner.ts` (hand-rolled `--- key: value ---` frontmatter parser, scans `~/.claude/skills/*/SKILL.md` + `<cwd>/.claude/skills/*/SKILL.md`, project overrides user when slugs collide, injectable fs seam mirroring skill-installer.ts), `skill-npx-installer.ts` (Spawner-wrapped `npx skills add <ref> -g -y`, ref regex `^[a-zA-Z0-9][\w./@-]+$` hardening against shell:true injection, cwd = userData), `skill-finder.ts` (Spawner-wrapped `claude --dangerously-skip-permissions /find-skills <query>`, single-active-finder guard separate from ClaudeProcessManager so no collision with workflow runs, line-buffered stdout/stderr emit, cancel-kills-process). (B) IPC: extend `shared/ipc.ts` + `preload/index.ts` + `main/index.ts` with `skills:list`, `skills:install`, `skills:find-start` / `skills:find-output` (event) / `skills:find-exit` (event) / `skills:find-cancel`, plus companion `shell:open-path` for opening a skill folder in OS file manager. (C) Renderer: `state/skills.ts` mirroring state/connections.ts; `views/Skills.tsx` page (DataTable Name · Description · Source · Actions=Open, Source rendered as Badge user/project, EmptyState CTA nudges installing `ef-feature` via pre-filled Find dialog); `components/FindSkillDialog.tsx` (query input testid `find-skill-search`, streamed monospace output area subscribing to skills:find-output, heuristic regex `^[\s•\-*]+\s*([a-z][a-z0-9-]+(?::[a-z][a-z0-9-]+)?)\s*[:—-]\s*(.+)$` extracts inline install cards with `find-skill-install-{ref}`, manual-install fallback input + button `find-skill-install-manual` for when regex misses, Cancel kills process); `IconSkills` sparkle in icons.tsx; Sidebar extends SidebarNavId with 'skills' between Connections and Settings; App.tsx adds `{kind:'skills'}` ViewState variant + route handler + render. 4 plan deviations to surface on review: (1) heuristic-regex install-card detection plus manual fallback input (ticket's "each recommended skill has inline Install button" not enforceable since `/find-skills` output format isn't our contract), (2) Uninstall action skipped (ticket marked it `?`, no testid), (3) row Open action implemented via new `shell:open-path` IPC, (4) install ref regex hardening. Tests: 6 new files — modules-skills-scanner, modules-skill-npx-installer, modules-skill-finder, state-skills, views-skills, components-find-skill-dialog. Out of scope per ticket: per-project default skill picker, per-run skill override.
- 2026-05-11T21:25Z [DONE] #GH-59 Global notification toast stack for run-finish + approval-while-elsewhere shipped via /ef-auto-feature. PR #61. Single bundled feature, single P0. Plan deviation from ticket-proposed zustand to React 18 useSyncExternalStore + module-level store (0 new deps, matches #9 hand-rolled tokenizer / no-Redux precedent) explicitly surfaced in the plan-review marker and accepted by the reviewer. (A) Store `src/renderer/state/notifications.ts`: dispatchToast / dismissToast / dismissToastByKey / dismissAllToasts / useNotifications (useSyncExternalStore against a module-level ReadonlyArray<Toast>); dedupeKey collision updates in place preserving id + createdAt; counter-based ids; __resetNotificationsForTests helper. ToastType union covers success | error | warning | info | approval with ToastAction { label, onClick, variant? }. Auto-dismiss timers live in ToastStack (per-toast useEffect on ttlMs), store stays pure. (B) Components: Toast.tsx (glass card body + type-tinted left accent bar via [data-type] selectors + ARIA role=alert/aria-live=assertive for error/approval, role=status/polite otherwise + size={18} on all type icons so success doesn't render visually smaller than error/approval); ToastStack.tsx (fixed bottom-right, column-reverse so newest paints lowest visually, pointer-events: none on container + auto on each toast for click-pass-through, max 4 visible via slice(-maxVisible) keeping the newest, per-toast setTimeout map keyed by id with full cleanup on unmount + cancel-on-removal); CSS uses tokens (--bg-card-elevated, --border-default, --accent / --success / --danger / --warning) — zero raw hex; entry slide+fade 200ms with prefers-reduced-motion fallback to opacity-only; approval type gets a 2.6s pulsing accent-soft box-shadow border; body max-height clamped to ~6 lines so a multi-line stack trace can't stretch the toast off-viewport. (C) Dispatchers `state/notification-dispatchers.ts`: useNotificationDispatchers({ currentExecutionRunId, onNavigateToExecution }) subscribes to runs.onCurrentChanged, tracks Map<runId, { state, approvalKey, terminalFired }> for once-per-run terminal trigger (success+OpenPR action + View run, 8 000ms ttl on done; error + View run, no ttl on failed; warning + View run, 5 000ms ttl on cancelled); approval trigger on pendingApproval null → set fires approval toast (dedupeKey approval-{runId}) ONLY when currentExecutionRunId !== run.id, with Approve / Reject actions calling window.api.runs.approve / reject (same IPC the ApprovalPanel uses) + View details action; approval set → null dismisses matching toast; separate effect dismisses matching approval toast when currentExecutionRunId becomes the runId (navigate-into-ExecutionView gesture); approvalIdentity stable hash via JSON.stringify(raw) detects payload refresh → dedupe replaces in place. Open PR action uses window.open(prUrl, '_blank') which is intercepted by main's setWindowOpenHandler → shell.openExternal (same path as run-history "View" anchor) — clarifying comment added in-PR. (D) App.tsx mounts <ToastStack /> after <AppShell /> and calls useNotificationDispatchers with currentExecutionRunId derived from view.kind === 'execution' ? view.runId : null and onNavigateToExecution wrapping setView. 1005/1005 unit tests pass + 12 pre-existing skipped (was 973 baseline at #GH-57; +32 new across 4 files — NOTIF-STORE-001..009 store ops + dedupe + idempotent dismiss; CMP-TOAST-001..007 rendering + ARIA + close + actions; CMP-TOAST-STACK-001..006 empty-marker + store-subscribed render + max-4 clamp keeps newest + positive-ttl schedules dismiss + non-positive ttl is a no-op + early-remove cancels pending timer; NOTIF-DISPATCH-001..010 done+prUrl→success / failed→error+no ttl / cancelled→warning+5s ttl / terminal-fires-once-on-repeat-snapshot / approval-off-route → toast / approval-on-matching-ExecutionView → suppressed / approval set→null → dismiss / navigate-to-ExecutionView → dismiss / payload-change → dedupe-refresh / Approve-action → runs.approve IPC). Reviewer verdict: Ready for PR; 3 in-PR nits applied (body max-height clamp + consistent icon size=18 + window.open clarifying comment); remaining 3 suggestions (memo Map unbounded growth across session, dismiss closure capture if future caller passes fresh prop each render, __resetNotificationsForTests clearing listeners is brittle if cleanup order shifts) explicitly accepted as acceptable trade-offs / future-proofing. Visual outcomes flagged as manual `npm run dev` smoke items in testBaseline.md — bottom-right anchor stacks above all routes, entry slide+fade and reduced-motion fallback, approval pulse, pointer-events pass-through gaps, and the cross-route live flow (start run → navigate elsewhere → approval marker → toast → Approve → resume).
- 2026-05-11T20:30Z [PLAN] #GH-59 Global notification system — toast stack for run-finish + approval-while-elsewhere planned: single P0 across two lanes. Module-level notification store (no new dep — deviating from ticket's zustand proposal by using React 18 useSyncExternalStore against a module-level store, matches the project's hand-rolled #9 tokenizer / no-Redux precedent). (A) Store: `src/renderer/state/notifications.ts` exports a `NotificationStore` (toasts: Toast[], dispatch, dismiss, dismissAll) with dedupeKey → in-place update and stable id/createdAt stamping; `useNotifications()` hook via useSyncExternalStore. Toast type union: 'success' | 'error' | 'warning' | 'info' | 'approval'. Auto-dismiss timer ownership lives in the ToastStack (per-toast useEffect on ttlMs), so the store stays pure. (B) Dispatchers: `src/renderer/state/notification-dispatchers.ts` exports `useNotificationDispatchers({ currentExecutionRunId, onNavigateToExecution })` that subscribes to `runs.onCurrentChanged`, tracks per-runId last-seen state + approvalRaw via refs, fires terminal-state toast once (success on done + Open PR action if prUrl, error on failed no auto-dismiss, warning on cancelled 5s), and approval toast on pendingApproval null→set when currentExecutionRunId !== runId. (C) Components: `Toast.tsx` (single toast, type-tinted accent bar, optional body/actions, `data-type=`, dismissible via × button); `ToastStack.tsx` (fixed bottom-right, column-reverse flex, max 4 visible, pointer-events: none on container / auto on each toast, prefers-reduced-motion flattens animations). (D) App wiring: `App.tsx` mounts `<ToastStack />` inside the shell root + calls `useNotificationDispatchers` deriving currentExecutionRunId from `view.kind === 'execution' ? view.runId : null` and onNavigateToExecution from setView. Tests: 4 new files — `state-notifications.test.ts` (store dispatch/dedupe/dismiss/dismissAll), `components-toast.test.tsx` (rendering + action callbacks + type attribute + close button), `components-toast-stack.test.tsx` (stacking order + max-4 + auto-dismiss timer + reduced-motion respect), `state-notification-dispatchers.test.tsx` (terminal-state trigger once-per-run + approval suppression when on matching ExecutionView + approval auto-dismiss on flip-back). Out of scope per ticket: history view, OS-level Notification API, Markdown body, multi-window mirroring.
- 2026-05-11T19:06Z [DONE] #GH-57 ActiveExecutionPanel + ProjectDetail Refresh + ExecutionLog UI polish (8 items) shipped via /ef-auto-feature. PR #60. Single bundled PR across all 8 ticket items in 2 lanes. Surface 1 (ActiveExecutionPanel / ProjectDetail) — (#1) Dropped `scrollbar-gutter: stable` on `.main[data-route='detail']` so the fixed `.activeCard` (max-width 1180px) and the centered `.page` (max-width 1180px) share the same effective viewport width and align pixel-perfect. (#2) Renamed in-panel approval testids `active-execution-approve/reject` → `panel-approve/panel-reject` (companion to ExecutionView's ApprovalPanel which uses `approve-button`/`reject-button`); `.activeHead` gains `flex-wrap: wrap` + `row-gap: var(--space-2)` so the Approve/Reject/Open-Details cluster can break onto a second line at narrow widths without crowding the status badge — same `runs.approve`/`runs.reject` IPC dispatch path as ApprovalPanel, no duplicate handler. (#3) ProgressBar replaces the GH-52 opacity-breath with a sliding linear-gradient `::after` overlay on `.fill[data-running='true']`. Cadence is one highlight crest per 2.4s sweep (`background-size: 100% 100%`, position `100% 0` → `-100% 0` over 2.4s linear) — the initial review-pre fix had a 200% → -200% range which would have shown two crests per cycle (2× faster than the ticket's "~2.4s" guidance), corrected in-PR. `data-running` prop hook preserved so CMP-PROG-BREATH-001..003 stayed green; `prefers-reduced-motion` flattens to no overlay. (#5) `.refreshSpinning` promoted from a styleless inline `<span>` to `display: inline-flex; width: 14px; height: 14px; vertical-align: middle; transform-origin: center center;` so the IconRefresh glyph rotates around a stable pivot inside the Button's text baseline (root cause: the previous inline `<span>`'s bounding box drifted with the surrounding line-box per rotation frame, tracing an ellipse instead of spinning in place). Surface 2 (ExecutionLog) — (#4a) Added `line-height: 1` on `.head` children (`.label`, `.range`, `.heartbeat`) so display-font baseline padding doesn't push the text off the icon's vertical centerline; the existing `align-items: center` on `.head` already centred boxes — this fix kills the text-box vertical slack. (#4b) Left accent bar widened 3 → 4px, switched to `var(--radius-pill)`, softened the running-state `barBreath` box-shadow (16px max → 12px max, less halo against the new #49 tokens), and mask-faded top/bottom for done/failed/cancelled rows via `mask-image: linear-gradient(180deg, transparent 0%, black 14%, black 86%, transparent 100%)` so terminal bars don't feel as visually loud as the active one. (#4c) Sticky `.head` extends left to butt against the new 4px accent bar (`margin-left: calc(-1 * var(--space-4) + 4px)`, compensating `padding-left: calc(var(--space-4) - 4px)` keeps the content centered against the body), plus a solid `border-bottom: 1px solid var(--border-subtle)` for a clear sticky-boundary cue when scrolled. The running-row's accent-soft → bg-card head gradient still works over the wider extent. (#4d) New `.scrollWrap` relative wrapper now contains `.scroll` + a floating scroll-to-bottom button (`.scrollFab`, `data-testid="log-scroll-to-bottom"`, IconChevronDown glyph, 36×36, accent-tinted). Visible only when `distanceBelowViewport > 100px` AND `followRef === false`; click calls `scrollTo({behavior: 'smooth'})` + re-engages `followRef = true` + clears the visibility flag so subsequent streamed lines stay auto-followed. `tabIndex={-1}` + `aria-hidden` when hidden so the FAB is out of the tab order. `pointer-events: none` on the hidden state so it can't be clicked through. 973/973 unit tests pass + 12 pre-existing skipped (was 970 baseline at #GH-13; +3 new — CMP-EXEC-LOG-FAB-001..003 covering hidden-at-bottom / visible-after-scroll-up / click-scrolls-and-rehydes; the 3 DET-WIDGET-APPROVE testid refs were updated in place). Reviewer verdict: Approve with minor polish; 1 warning landed as in-PR fix (progress shimmer cadence corrected from 200% range to 100% range). 5 of the 8 items (#1 panel/page edge alignment, #3 sliding gradient, #4b accent bar mask-fade, #4c sticky head visual, #5 refresh in-place rotation) are visual-only and flagged in testBaseline.md as manual `npm run dev` smoke items (jsdom can't compute `scrollbar-gutter`, `mask-image`, CSS animations, or `position: sticky` layout). The remaining 3 items have behavioral hooks covered by unit tests (`panel-approve`/`panel-reject` testid + IPC dispatch, `data-running` attribute plumbing, FAB visibility/click state-changes).
- 2026-05-11T18:54Z [PLAN] #GH-57 UI bugs in ActiveExecutionPanel + ProjectDetail Refresh + ExecutionLog (8 items) planned: single bundled PR (matches GH-52 precedent + PR-bundling default). Surface 1 — ActiveExecutionPanel/ProjectDetail: align active-panel right edge with page by dropping scrollbar-gutter on detail route (#1); rename in-panel testids active-execution-approve/reject → panel-approve/panel-reject + allow .activeHead to flex-wrap so buttons stop crowding the badge (#2); ProgressBar swaps opacity-breath for a sliding linear-gradient ::after overlay while data-running='true' (prefers-reduced-motion still flattens, CMP-PROG-BREATH-001..003 unchanged) (#3); .refreshSpinning becomes inline-flex 14×14 with transform-origin: center + vertical-align: middle so IconRefresh rotates in place (#5). Surface 2 — ExecutionLog: tighten .head children line-height: 1 (#4a); widen left accent bar 3→4px, pill radius, soften running halo, mask-fade top/bottom for terminal states (#4b); extend sticky .head left margin to the accent bar + add solid bottom-border for sticky boundary cue (#4c); add scroll-to-bottom FAB (.scrollFab, testid log-scroll-to-bottom, IconChevronDown, position: absolute inside new .scrollWrap, visible when scrollHeight - scrollTop - clientHeight > 100) (#4d). Tests: rename DET-WIDGET-APPROVE testids + add CMP-EXEC-LOG-FAB-001..003.
- 2026-05-11T18:35Z [DONE] #GH-13 Ticket locking to prevent duplicate execution shipped via /ef-auto-feature. Single P0 across three lanes. (A) RunHistory schema v1→v2: each running entry is now `{ key, lockedAt }` instead of a bare string; v1 files (`running: string[]`) read successfully and migrate each entry to `lockedAt: 0` (the "definitely stale" sentinel), upgraded on the next mutation. New `getRunningWithMetadata()` returns timestamps; `getRunning(projectId)` keeps its `ReadonlyArray<string>` contract so the poller's `new Set(getRunning(id))` filter stays untouched. New `releaseStaleLocks(thresholdMs)` clamps negative thresholds to 0 (defense against a buggy computed value wiping fresh locks), returns released entries. Injectable clock for tests, mirroring the WorkflowRunner pattern. (B) WorkflowRunner.start() adds a cross-session lock pre-check between project lookup and ctx creation: if `runHistory.getRunningWithMetadata(projectId)` carries an entry for the ticketKey, return ALREADY_RUNNING with a distinct message ("ticket X is already locked (started Y); check the app logs or remove the entry from run-history.json") — separate from the in-process `this.active !== null` rejection ("a run is already active"). v1-migrated locks render the sentinel as "started before the last app restart" instead of an ISO timestamp. (C) initStores() runs `releaseStaleLocks(0)` immediately after `runHistory.init()` and before constructing the TicketPoller / WorkflowRunner, so the poller's bootstrap first-poll cannot observe stale locks. Each released entry is console.warn'd with projectId + key + lockedAt; UI surfacing is deferred. Polling-side filter was already done pre-GH-13 (ticket-poller's listPage + runPoll already drop `runHistory.getRunning(projectId)` entries) — no behavior change there. 970/970 unit tests pass + 12 pre-existing skipped (was 954 baseline at #GH-52; +16 net new across 2 files: 11 new in run-history.test.ts covering lockedAt stamping, getRunningWithMetadata, re-mark preserves original timestamp, releaseStaleLocks(0/threshold) semantics, v1 file migration + on-disk v2 rewrite, no-op write on empty store, persistence across re-init, negative-threshold clamp, future lockedAt clock-skew preservation; 5 new in workflow-runner.test.ts WFR-CROSS-SESSION-LOCK covering reject-on-same-ticket / allow-on-different-ticket / allow-on-different-project / message-wording-asserts-no-restart-claim / sentinel-lockedAt=0 message branch). Reviewer verdict: Ready for PR; 2 warnings landed as in-PR fixes (negative-threshold clamp at `Math.max(0, thresholdMs)` so a buggy caller can't accidentally release fresh locks; reworded the cross-session ALREADY_RUNNING message away from the contradictory "restart the app to clear stale locks" advice — restart already ran releaseStaleLocks at startup, so if the user sees this message a restart won't help). Overloaded ALREADY_RUNNING error code (covers both in-process duplicate and cross-session orphan) and deferred test gaps (assertion on the v1-migrated message branch was actually added) explicitly accepted as acceptable trade-offs for MVP. Out of scope: per-lock TTL configuration, manual `runs:release-lock` IPC, UI surfacing of released locks (the console.warn path satisfies the spec's "auto-released OR flagged" criterion).
- 2026-05-11T18:22Z [PLAN] #GH-13 Ticket locking to prevent duplicate execution planned: single P0 across three lanes — (A) RunHistory schema v2 with lockedAt timestamps per running entry, back-compat read of v1 string[] (stamped lockedAt=0 as "definitely stale" sentinel), new releaseStaleLocks(thresholdMs) method, getRunning(projectId) signature preserved as `ReadonlyArray<string>` so the poller filter stays unchanged. (B) WorkflowRunner.start() pre-checks `runHistory.getRunning()` for the ticketKey BEFORE markRunning, returns ALREADY_RUNNING with cross-session-aware message when an orphaned lock exists. (C) initStores() runs releaseStaleLocks(0) right after runHistory.init() and console.warns released entries (renderer surfacing deferred — console log satisfies spec's "auto-released OR flagged"). Polling-side skip is already done (filter via getRunning in ticket-poller's listPage + runPoll) — regression test added. Out of scope: per-lock TTL configuration, manual runs:release-lock IPC, UI surfacing of released locks.
- 2026-05-09T01:19Z [DONE] #GH-52 Bugs in small ticket-runner widget + ExecutionView + step-state model + skill-phase coverage shipped via /ef-auto-feature. PR #56. Single bundled PR across all 8 ticket items in 5 lanes. (A) Schema + runner: 6 new RunStates added (fetchingTicket, understandingContext, planning, implementing, evaluatingTests, reviewingCode); `running` flipped to runner-internal umbrella (hidden in UI); transitionToPhase dedupes consecutive same-phase markers (no-op when state + last-running-step + incoming phase all match); dispatchApproval closes the prior in-flight step BEFORE pushing awaitingApproval (extracted to closeLastRunningStep helper, reused by both transition paths) so the timeline never shows two simultaneously-running steps. (B) Skill: ef-auto-feature/SKILL.md emits phase markers at the start of all 11 phases with explicit "emit ONCE, at the START of the named action" contract; the committing marker now fires immediately before `git commit` instead of before `git add`. (C) Renderer state tables: USER_VISIBLE_LABELS in run-log.ts + RUN_STATE_LABELS + STATE_PROGRESS_ORDER in ProjectDetail.tsx all extended for the new states; awaitingApproval collapses to `planning` (most common pre-approval phase) so the bar doesn't snap backward to the umbrella state. (D) Widget UX: ActiveExecutionPanel extracted from ProjectDetail; useRunLog drives a single-line latest-output ticker via LogPreview (no more permanent "Awaiting output…"); ProgressBar gains `running` prop with 3.2s ease-in-out glow breath on the fill (prefers-reduced-motion respected); inline Approve / Reject buttons in the widget when Run.pendingApproval is non-null, dispatching the same runs.approve / runs.reject IPC paths the ApprovalPanel uses; per-row data-awaiting-approval attribute. (E) ExecutionLog UX: auto-follow accordion via a separate userOpenedSteps Set — when expandIndex advances and autoScroll is on, the previously-active step collapses unless the user manually toggled it; rows expose data-auto-managed for tests; sticky `.head` via position: sticky + explicit background, with running rows getting a tinted accent-soft → bg-card gradient on the sticky head so the visual transition into the running gradient body is smooth instead of a solid pop. 954/954 unit tests pass + 12 pre-existing skipped (was 938 baseline at #49; +16 new across workflow-runner.test.ts WFR-040/041/042 covering close-prior + dedupe + new-phase-marker walk, components-execution-log.test.tsx CMP-EXEC-LOG-AUTO-FOLLOW/USER-OPEN/AUTO-OFF, components-progress.test.tsx CMP-PROG-BREATH-001..003, views-project-detail.test.tsx DET-WIDGET-BREATH/NO-BREATH/APPROVE/APPROVE-CLICK, new tests/unit/skill-markers.test.ts SKILL-MARKERS-001..003 static-analysis guard against skill ↔ runner phase-value drift, plus updated RUN-SCHEMA-001 to enumerate the 20 RunStates). Reviewer verdict: Ready for PR; 3 in-PR fixes applied (extracted closeLastRunningStep to dedupe identical 4-line block in transitionToPhase + dispatchApproval, tightened skill-markers regex to allow digits/underscores in phase names for forward safety, tinted sticky `.head` with accent-soft gradient on running rows so scrolling past the sticky doesn't visually pop). systemPatterns.md updated: phase marker contract section enumerates the new 11-value PHASE_VALUES set, documents the close-prior-step + dedupe semantics, and points at skill-markers.test.ts as the drift guard. Sticky-header visual + inline-approve full-flow live behavior flagged as manual `npm run dev` smoke test items in testBaseline.md (jsdom doesn't compute layout for `position: sticky`).
- 2026-05-09T00:46Z [DONE] #49 UI refinement — ground color system in design source shipped via /ef-auto-feature. PR #55. Pure visual / token-layer change, no behavior delta. Re-anchored both `:root[data-theme='dark']` and `:root[data-theme='light']` in `src/renderer/styles/tokens.css` to the navy palette from `design/add_project.png`: surface tiers step distinctly (app `#0a1224` < sidebar `#0d1530` < card `#15203a` < card-elevated `#1c2a4a` < input `#0e1a35`); accent brightens to `#5b8dff` to match `PaperplaneGlyph` body fill (theme accent IS the brand); warning warms to amber (`#f4c264` dark / `#d99a55` light); light theme re-grounded as a credible inversion with cool-tinted bg lifting white cards. New `--danger-deep` token added to mirror the existing `--accent-deep` brand-depth pattern, consumed by `RunStatusFigure.module.css` failed-state shadow (was hard-coded `#a13d3d`). Mesh gradients in `reset.css` re-tinted to follow new `--accent` rgba base (`rgba(91,141,255,...)`). `BrowserWindow.backgroundColor` synced to `#0a1224` so Electron's pre-paint launch flash matches. `ExecutionLog.module.css` dropped its dead `var(--warning, #d99a55)` fallback. 938/938 unit tests pass + 12 pre-existing skipped (was 936 baseline at #51; +2 new in `tokens.test.ts` — TOKEN-006 brand-alignment lock on dark `--accent`/`--accent-deep` against `PaperplaneGlyph` brand fills, TOKEN-007 regression guard on `RunStatusFigure` failed `.shadow` tokenization). Reviewer verdict: Ready for PR; 2 warnings landed as in-PR fixes — pinned all four light-theme accent extras to a coherent `(91, 141, 255)` rgba base for cross-theme parity; brought dark `--accent-soft` (0.16 → 0.12) and `--accent-glow` (0.55 → 0.45) onto the issue's proposed opacities so the accent doesn't read too loud against the new bg. Brand glyph fills in `PaperplaneGlyph.tsx` and `RunStatusFigure.module.css` (lines 36, 40) deliberately stay literal — brand-locked, not theme-dependent. Smoke test in `npm run dev` not run in this autonomous run; flagged as a manual eye-check needed before merge.
- 2026-05-08T23:34Z [DONE] #51 Paperplane brand wire-up + product rename + userData migration shipped via /ef-auto-feature. PR #54. Three lanes: (1) brand assets — IconLogo body swap to two-tone paperplane silhouette, Sidebar + Titlebar render an inline horizontal lockup whose wordmark uses currentColor (theme-driven without re-render), new RunStatusFigure in ExecutionView header plays a 5s Lottie loop while pending/running/awaitingApproval and swaps to a tinted static glyph on terminal status (done = brand colors, failed = --danger, cancelled = --text-tertiary), prefers-reduced-motion: reduce honored. (2) Product rename — productName → Paperplane, appId → tech.emonster.paperplane, BrowserWindow title, <title>, package.json description, README + prd callout. Repo + npm name stay e-frank (intentional divergence). (3) src/main/modules/migrate-userdata.ts runs once before initStores() in app.whenReady() — copies legacy %APPDATA%\e-frank\ tree into the renamed Paperplane userData dir on first launch, idempotent via migrated-from-efrank.json marker, partial-failure tolerant, never deletes legacy, appends migration.log block per run. New PaperplaneGlyph component is the single source of truth for the silhouette across IconLogo + Sidebar + Titlebar + RunStatusFigure (drift-guard test asserts runtime coords match design SVG). Adds lottie-react + lottie-web (pure JS); global vitest mock keeps lottie-web's canvas probe out of jsdom. 936/948 unit tests pass + 12 pre-existing skipped (was 911 baseline; +25 new across PaperplaneGlyph drift guard, RunStatusFigure status branches + reduced-motion, SidebarLockup wiring, migrate-userdata behavior). Reviewer verdict: Fix critical issue first (sidebar lockup polygon coordinate drift between updated Titlebar/IconLogo and stale Sidebar inline polygons) → fixed pre-merge by extracting PaperplaneGlyph + wiring all 4 sites + drift-guard test; remaining warnings (copyTree symlink follow, hard-coded .tag indent matching SVG x="42") explicitly accepted as acceptable trade-offs.
- 2026-05-08T23:06Z [DONE] #50 Custom draggable titlebar (frameless window + 32px paperplane bar) shipped via /ef-auto-feature. New `chrome:*` IPC namespace (minimize/maximize/close/get-state + state-changed event) wired through shared/ipc → preload → main, where each handler resolves its target via `BrowserWindow.fromWebContents(event.sender)`. Main: `frame:false` on Win/Linux, `titleBarStyle:'hiddenInset'` + `trafficLightPosition:{x:12,y:9}` on macOS, broadcasts maximize/unmaximize. Renderer: new `<Titlebar/>` component renders the paperplane lockup left + drag region + min/max/close (Win/Linux only); inline SVG wordmark uses `currentColor`/`--text-primary` so theme changes propagate without `useTheme()` (which is per-component state). 911/911 unit tests pass + 18 new (12 IPC contract, 6 component). Reviewer verdict: Approve with required follow-up on Alt+Space (frameless-Electron quirk requiring native bindings — explicitly deferred and documented in PR body); 3 in-PR fixes applied (macOS traffic-light alignment, theme-drift via `currentColor` swap, getState/onStateChanged race hardening).
- 2026-05-08T14:38Z [DONE] #45 Connections empty-state copy alignment shipped via /ef-auto-feature. PR #48. Empty-state was already wired in `Connections.tsx`; aligned the description (now references "credentials e-frank uses to fetch tickets and open PRs (Jira API tokens, GitHub PATs)") and CTA label ("Add connection") to the ticket spec; tightened `VIEW-CONN-002` to assert exact copy + added `VIEW-CONN-002b` for CTA→AddConnectionDialog flow. 894/894 unit tests pass, no regressions.
- 2026-05-08T00:00Z [PLAN] #44 Copy Branch button planned: single small UI tweak — add branch label + copy button to active execution panel (`ProjectDetail.tsx` + module.css). New `IconClipboard` icon. `data-testid=active-execution-copy-branch`. 1.5s "Copied" affordance, silent failure on `navigator.clipboard` unavailable.
- 2026-05-06T15:00Z [DONE] #10 Git Manager complete. 886/889 unit tests pass + 3 pre-existing skipped (was 845 after #25; +45 new in git-manager.test.ts). NodeGitManager wraps the git CLI via the Spawner abstraction; new error codes (CONFLICT, DIRTY_TREE, BRANCH_EXISTS, AUTH_FAILED, NO_CHANGES, TIMEOUT) with actionable details (file lists for conflicts/dirty trees, capped + scrubbed stderr). Per-op timeouts (10s local, 60s pull/push) with SIGTERM → 500ms grace → SIGKILL escalation. shell:false on every git invocation neutralizes injection. main/index.ts swap from StubGitManager → NodeGitManager. Reviewer verdict: Ready for PR after fixing 1 warning (in-PR scrubStderr regex strips `https://user:token@host` userinfo before surfacing in details.stderr).
- 2026-05-06T13:00Z [PLAN] #10 Git Manager planned: single P0 — NodeGitManager class beside the existing StubGitManager, wraps `git` CLI via the Spawner abstraction. New error codes (CONFLICT, DIRTY_TREE, BRANCH_EXISTS, AUTH_FAILED, NO_CHANGES). Per-op timeouts. Conflict detection via stderr CONFLICT-line parsing. Hooks + GPG signing left enabled (no `--no-verify`, no `--no-gpg-sign`).
- 2026-05-06T11:00Z [DONE] #25 Refactor ProjectInstance to use Connections + picker UI complete. 750/753 unit tests pass + 3 pre-existing skipped (was 691 after #24; +59 net new across 9 test files). Schema break: RepoConfig drops host/tokenRef, gains connectionId+slug; TicketsConfig drops host/email/tokenRef, gains connectionId+projectKey, query made optional. JiraPoller resolves auth via ConnectionStore at poll time. JiraClient gains listProjects(); two new IPC channels (connections:list-repos, connections:list-jira-projects); main wires getReferencingProjectIds. AddProject UI rewrite: connection pickers + lazy-loaded resource pickers + Refresh + empty-state CTAs (inline AddConnectionDialog) + broken-connection banner for Edit mode. Reviewer verdict: Ready for PR after fixing 1 CSS warning (--warning-border token replacing hardcoded rgba). Phase 4 (#10-#13) now unblocked.
- 2026-05-06T09:00Z [PLAN] #25 Refactor ProjectInstance to use Connections + picker UI planned: single P0 across three lanes — (1) Schema break: RepoConfig/TicketsConfig drop credentials, gain connectionId + slug/projectKey; (2) Backend: JiraPoller resolves auth via ConnectionStore at poll time, JiraClient gains listProjects, two new IPC channels for resource picking, main wires getReferencingProjectIds to scan ProjectStore; (3) Renderer: AddProject rewrite to picker UI with per-session resource caches, empty-state CTAs, broken-ref handling in EditProject. Pre-MVP — clean schema break, no migration shim.
- 2026-05-06T07:00Z [DONE] #24 Connection model + Connections settings view complete. 659/662 unit tests pass + 3 skipped (was 535/538 after #9; +124 new across 7 test files: 22 schema + 22 store + 22 github-client + 27 ipc + 5 hook + 12 view+app + 11 dialog + 3 sidebar). Reviewer verdict: Ready for PR after fixing 1 warning (Jira email-only update path now reads + re-joins the existing token; secretsManager Pick widened to include `get`; new regression test CONN-STORE-010b). Bitbucket accepted in schema enum, NOT_IMPLEMENTED at runtime per spec.
- 2026-05-06T05:00Z [PLAN] #24 Connection model + Connections settings view planned: single P0 feature. New Connection schema + ConnectionStore mirroring ProjectStore patterns; minimal GithubClient (testConnection / listRepos); paste-a-token auth (PAT for GitHub, API token for Jira) — OAuth flows arrive in #26/#27. Sidebar nav item + Connections list view + Add/Edit dialog. Cascade-delete gate plumbed via injected getReferencingProjectIds (no-op until #25 adds reference fields). Bitbucket accepted in schema enum, NOT_IMPLEMENTED at runtime.
- 2026-05-05T08:30Z [DONE] #9 Approval Interface complete. 535/538 unit tests pass + 3 skipped (was 485 after #8; +50 new across 4 files: 13 syntax + 12 code-diff + 17 approval-panel + 8 EXEC-APPROVAL view tests). Reviewer verdict: Ready for PR after fixing 2 warnings (added `--accent-soft` / `--accent-border` design tokens to replace masked-by-fallback rgba values; added `sendTestId` prop to PromptInput so the panel-internal composer uses `approval-modify-input` / `approval-modify-send` and avoids collision with the page-bottom prompt). No new runtime deps; hand-rolled TS/JS/Python/Go tokenizer powers the diff highlighter.
- 2026-05-05T08:00Z [PLAN] #9 Approval Interface planned: single P0 renderer-only feature. New ApprovalPanel + CodeDiff components replace ExecutionView's right-pane placeholder; reuses PromptInput from #8 for the Modify flow; reads from Run.pendingApproval populated by #7's marker parser; hand-rolled TS/JS/Python/Go tokenizer (no new dep). Right pane hidden when pendingApproval === null.
- 2026-05-05T06:00Z [DONE] #5 Project List UI + design system foundation complete. 302/302 unit tests pass (was 271/271 after #4; +31 new across 6 files, plus 4 changed APP-001..006 replacing #1's FE-001..003). 12 design-system primitives, 2 views (ProjectList + AddProject), design tokens on `:root`, dark theme with General Sans + JetBrains Mono. Reviewer verdict: Ready for PR; 4 warnings fixed in-PR (Dialog focus management, hard-coded user identity removed, sidebar gradient + toggle thumb tokenized).
- 2026-05-05T05:30Z [PLAN] #5 Project List UI + design system foundation planned: 12 component primitives, 2 views (ProjectList, AddProject), design tokens on `:root`, dark theme with General Sans + JetBrains Mono. Replaces #1 placeholder ping demo. Defaulting branding to `e-frank`.
- 2026-05-05T05:00Z [DONE] #4 Jira client + ticket polling complete. 271/271 unit tests pass (was 172/172 after #3; +99 new across 6 files). Reviewer verdict Ready for PR after fixing 2 criticals (`tickets.host` field added to schema for production polling; `ticketsDiffer` made set-based to avoid spurious events on Jira reorderings) + 3 warnings (stoppedDueToAuth short-circuit in runPoll, cascade ordering in PROJECTS_DELETE, comment clarification on consecutiveErrors).
- 2026-05-05T03:30Z [PLAN] #4 Jira client + ticket polling planned: 5 modules (http-client abstraction, jira-client REST wrapper, run-history JSON store, jira-poller per-project scheduler, ticket schema). Mirrors prior testability patterns (Spawner, SecretsBackend → HttpClient).
- 2026-05-05T02:50Z [DONE] #3 Project Instance config + secrets storage complete. 172/172 unit tests pass (was 90/90 after #2; +82 new across 4 test files). Reviewer verdict Ready for PR after fixing 1 critical (SecretsManager init failure was silently overwriting corrupt secrets file — now leaves manager null + adds initialized guard) + 4 warnings (NOT_NUMBER code, ProjectStore CRUD init guards, cascade-delete dedup via Set, hide backend error from plaintext leakage path).
- 2026-05-05T01:00Z [PLAN] #3 Project Instance config + secrets storage planned: hand-rolled validator (no zod), JSON file with schema-versioned envelope + atomic writes + write mutex, `safeStorage`-backed SecretsManager via SecretsBackend abstraction (FakeBackend for tests, mirroring #2's Spawner pattern).
- 2026-05-04T09:30Z [DONE] #2 Claude Process Manager complete. 90/90 unit tests pass (added 66 from #1's 23 baseline + 1 drift-guard suite). Reviewer verdict Ready for PR after fixing CRLF stripping for Windows + adding shared↔manager type drift guard. New `Spawner` abstraction in `src/main/modules/spawner.ts` enables full unit testing without spawning real processes.
- 2026-05-04T09:00Z [PLAN] #2 Claude Process Manager planned: one P0 module with Spawner abstraction, single active run, line-buffered IPC streaming, cancel + timeout + stdin support.
- 2026-05-04T08:50Z [DONE] PR #15 (sandbox CJS preload follow-up fix) merged — `npm run dev` now exposes `window.api` correctly with `sandbox: true`.
- 2026-05-04T07:55Z [DONE] #1 Electron + React + Vite scaffold complete. 22/22 unit + 1/1 E2E placeholder pass. Lint + typecheck clean. Build clean. Reviewer verdict: Ready for PR after fixing preload path mismatch (preload built as `.mjs`, main referenced `.js`) — regression-guard test added.
- 2026-05-04T07:25Z [PLAN] #1 scaffold planned as single P0 feature: Electron + React + Vite + IPC + packaging + test configs.
- 2026-05-04T07:21Z [INIT] Memory bank created from PRD content. Stack chosen: Electron + React + TypeScript + Vite + Vitest + Playwright.
- 2026-05-04T07:18Z [INIT] Repo bootstrapped: initial commit on `main`, PRD + ef-* skills + .gitignore. 13 GitHub issues opened covering MVP phases.

## Planned
- [ ] **#1 Electron + React scaffold** (Phase 1) — main process, renderer, dev tooling, IPC, packaging
- [ ] **#2–#3** Jira polling + ticket list UI (Phase 2)
- [ ] **#4–#7** Workflow execution, streaming logs, approval UI (Phase 3)
- [ ] **#8–#11** Git ops, PR creation, Jira update (Phase 4)
- [ ] **#12–#13** Polish / packaging
